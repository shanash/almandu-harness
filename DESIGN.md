# module-harness 이후 작업 설계 (2026-09-12)

이 문서는 kod-remastered 에서 분리된 시점부터의 계획이다. 결정과 그 근거를 적고, 결정되지
않은 것은 결정되지 않았다고 적는다. 작업이 끝나면 결과를 `observations/` 가 받고, 계약이
바뀌면 `MODULE.md` 이력이 받는다 — 이 파일은 둘 중 어느 것도 대신하지 않는다.

문서를 한국어로 쓰는 이유: 이 리포의 계약서·스키마·관찰이 전부 한국어이고, 이 문서는 그
문장들을 그대로 인용한다. 언어를 섞으면 인용이 번역을 거치게 된다.

---

## 1. 지금 있는 것

분리 직후 상태다. 아래는 전부 이 리포에서 확인한 것이다.

| 것 | 상태 |
|---|---|
| 스키마 | `MODULE-schema-v1.md` — 6필드 고정, 3개월 필드 추가 금지 규칙이 살아 있다 |
| 게이트 | `module-gate.mjs` — R0~R14, 외부 의존 0, 판정은 종료 코드로만 (module-gate.mjs:536,540) |
| 테스트 | `module-gate.test.mjs` — 45개, 임시 git 저장소 fixture, 이 리포 계약서를 입력으로 쓰지 않는다 |
| 관찰 | `observations/` 2건 — 규칙이 왜 생겼는지의 유일한 출처 |
| 소비 리포 | kod-remastered 가 `file:../module-harness` 로 설치, 12개 계약 전부 `status: active`, am-gate.json 에 blocking 으로 등록 |
| 어댑터 로드 | Read 는 조상 체인 전부 로드, **Bash·Write 는 로드 안 됨** (README 표) |

앞선 계획의 1~3단계(스키마 확정 → 게이트 → 어댑터 로드 검증)는 여기서 끝났다.
남은 것은 4단계(루프)와 5단계(리뷰), 그리고 패키지가 되면서 새로 생긴 운영이다.

### 아직 안 닫힌 구멍 하나 — 4단계가 존재하는 이유

게이트는 **커밋 시점에만** 서 있다. 코드를 고치는 순간에는 아무것도 서 있지 않고,
어댑터는 Read 로 파일에 닿을 때만 실린다. 즉 에이전트가 `cat` 으로 읽고 `Write` 로 고치면
계약을 한 줄도 안 본 채로 코드가 바뀌고, 그 사실은 커밋 때서야 드러난다.

README 는 이 구멍을 "사람과 에이전트의 습관에 기대고 있다" 고 적었다. 4단계의 목적은
그 습관을 메커니즘으로 바꾸는 것이다 — 루프가 변경 **전에** 소유 계약을 강제로 적재한다.
새 기능을 얹는 게 아니라 이미 아는 구멍을 닫는 일이다.

---

## 2. 순서

| 단계 | 하는 것 | 끝났다고 말할 수 있는 조건 | 상태 |
|---|---|---|---|
| 0 | 분리 잔재 정리 | 리포 안에 `.harness/` 접두사가 남지 않는다 (observations 제외) | 끝남 `ab25eb9` |
| 4a | 게이트의 기계 판독 출력 (`--json`, `--scope`) | 루프가 텍스트를 파싱하지 않고 게이트를 부른다 | 끝남 `ad01d04` |
| 4b | scope → change → reconcile 루프 | 이 리포 자신의 게이트 수정 한 건이 루프만으로 커밋까지 간다 | 끝남 `322d1e6` (observations/03 §1) |
| 5 | 계약 기반 리뷰 | 리뷰 지적이 전부 특정 불변식 ID 를 인용한다 | 끝남 (2026-09-17) — 구조는 섰고 오탐 없이 돌았지만 실전 검출로 값을 입증하지는 못했다. 세 질문 중 둘을 내리고 불변식 판정자 하나를 남겼다 (DESIGN-review 3·10절, observations/05 결론). 왼쪽 조건은 DESIGN-review 가 대체했고 종료를 판단한 것은 그 10절이다 |
| 운영 | 버전·배포·CI·설치 | 소비 리포가 태그로 고정해서 설치한다 | 도구·CI 는 끝남 `686f8c1`. 소비 리포 전환은 남았다 |

0 은 즉시. 4a 는 4b 의 선행이다. 운영은 4b 와 병행할 수 있지만 **버전 규칙만은 4a 전에**
정해야 한다 — `--json` 의 출력 모양이 공개 계약이 되는 순간부터 바꾸는 값이 비싸진다.

5 를 마지막에 두는 이유는 전과 같다. 리뷰의 입력(계약 + diff)이 흔들리면 리뷰어가 무엇을
기준으로 판단하는지가 매번 달라진다.

---

## 3. 0단계 — 분리 잔재

분리 때 경로가 `.harness/` 에서 리포 루트로 바뀌었는데 문서 일부가 옛 경로를 그대로 쓴다.

- `module-gate.mjs:3-7` 사용법 주석 — `node .harness/module-gate.mjs` → `npx module-gate`
- `MODULE-schema-v1.md:5,173,228` — `.harness/observations/`, `.harness/module-gate.mjs`, `.harness/MODULE.md`
- `module-gate.test.mjs:3,7` — 실행 경로와 "분리될 때 따라간다" 문장 (분리는 이미 일어났다)
- `observations/` 는 **고치지 않는다**. 당시의 기록이고, 관찰은 규칙이 아니라 증거다

스키마를 고치므로 R1 이 이 리포 계약서의 변경을 요구하고(watch 에 `MODULE-schema-v1.md`
가 있다), 계약 섹션을 건드리면 R2 가 이력 한 줄을 요구한다. 0단계는 게이트가 자기 자신에게
거는 이 요구를 사람이 손으로 처리하는 마지막 커밋이 된다 — 4b 이후에는 루프가 한다.

---

## 4. 4a단계 — 게이트의 기계 판독 출력

### `--json`

루프가 게이트 결과를 읽어야 하는데 지금 출력은 사람이 읽는 텍스트 줄이다. 다행히 내부
표현은 이미 구조체다 — `report()` 가 `{ level, mod, rule, msg }` 를 푸시한다
(module-gate.mjs:241). `--json` 은 그 배열을 직렬화하는 것 이상이 아니다.

```json
{
  "schema": 1,
  "mode": "worktree|staged|base|audit",
  "modules": 12,
  "changed": 7,
  "results": [{ "level": "FAIL", "module": "engine", "rule": "R1", "message": "..." }],
  "fail": 1,
  "warn": 2
}
```

지켜야 할 것 둘:

- **종료 코드는 그대로다.** I2 는 "차단 수단은 종료 코드뿐" 이라고 못박았다. `--json` 은
  같은 판정을 다른 표면으로 낼 뿐이고, 루프가 JSON 을 읽고 스스로 통과를 선언하는 구조가
  되면 I2 가 깨진다. 루프도 종료 코드로 막힌다
- **stdout 으로만 낸다.** I5 는 게이트가 자기 판단을 파일에 쓰지 않는다고 적었다.
  `--json --out report.json` 같은 것을 만들면 그 불변식이 깨진다. 파일이 필요하면
  부르는 쪽이 리다이렉트한다

`message` 는 한국어 문장이고 계속 바뀐다. 루프는 `rule` + `module` 로만 분기하고 `message`
는 사람에게 전달만 한다 — 메시지 문자열에 기대는 순간 게이트의 문구 수정이 루프를 깬다.

### `--scope <path>...`

"이 경로를 고치려면 어떤 계약을 읽어야 하는가" 만 답한다. 판정하지 않는다.

게이트는 이미 `ownerOf()` 로 가장 깊은 소유 모듈을 안다(module-gate.mjs:113). 그 함수를
CLI 표면으로 한 번 더 내는 것이고, 답은 소유 MODULE.md 와 그 조상들의 경로 목록이다.

이게 4b 의 scope 단계 전부다. 루프는 이 목록을 받아 **Read 로** 연다 — Read 여야 어댑터가
함께 실린다. 1절의 구멍은 여기서 닫힌다.

### 아직 결정하지 않은 것

`--scope` 가 조상 계약까지 낼지, 가장 깊은 것 하나만 낼지. 조상까지 내면 루트 계약이 매번
컨텍스트에 실려 깊은 파일 하나를 고치는 데 계약 넷이 온다. 하나만 내면 "부모-자식은 링크로
표현하지 않는다" 는 스키마 규칙 때문에 루프가 조상의 존재를 모른다. 4b 를 돌려 보고 정한다.

구현된 게이트는 체인을 깊은 것부터 통째로 낸다(2026-09-12). 질문에 답한 것이 아니라 답할 자리를
옮긴 것이다 — 부르는 쪽이 앞에서 잘라 쓸 수 있으므로 어느 쪽으로 정하든 게이트는 안 바뀐다.
반대로 하나만 냈다면 4b 가 조상을 원할 때 게이트를 다시 고쳐야 했다.

---

## 5. 4b단계 — scope → change → reconcile 루프

### 세 단계의 입출력

| 단계 | 입력 | 하는 일 | 출력 |
|---|---|---|---|
| scope | 사용자 요청 | 건드릴 경로 확정 → `--scope` → 계약을 Read 로 적재 | 대상 경로 + 적재된 계약 목록 |
| change | 요청 + 계약 | 구현 | 코드 diff |
| reconcile | 코드 diff + 계약 | `--json` 판정, 불변식이 흔들렸으면 계약 갱신 요구 | 종료 코드 0 인 변경 묶음 |

reconcile 이 판단하는 것은 게이트가 못 보는 쪽이다. 게이트는 "계약서를 안 고쳤다" 는
볼 수 있어도 "고친 계약서가 사실인가" 는 못 본다. observations 의 [규칙 부족-1] 이 실제로
그렇게 놓쳤다 — MODULE.md 가 다른 이유로 바뀌면 R11 이 눈을 감는다. reconcile 은 그
자리에서 "이 변경이 어느 불변식을 약화시켰나" 를 묻는다.

### 기준선 문제

루프가 게이트를 한 번만 돌리면 자기 변경이 만든 경고와 원래 서 있던 경고를 구분하지
못한다. kod-remastered 는 분리 전 한때 8 WARN 을 깔고 있었고, 그때 만든 기준선 파일은
규칙이 바뀌자 곧바로 거짓이 됐다(observations/01 갱신 표).

기준선을 파일로 저장하지 않는다. reconcile 이 변경 전후로 게이트를 두 번 돌리고
`(module, rule)` 쌍의 차집합만 본다. 느리지만(측정치 0.92~1.01s/12모듈) 낡지 않는다.

### R1 과 커밋 분할의 충돌 — 루프가 알아야 하는 규칙

observations 의 [규칙 충돌-1] 이 적은 것이다. "커밋은 기능 단위로 쪼갠다" 와 "계약 갱신은
별도 커밋" 은 `status: active` 모듈에서 성립하지 않는다 — R1 이 코드와 계약을 같은 변경
묶음에 요구하기 때문이다. 그 관찰은 "한 줄이 필요하다" 로 끝났고 아직 아무도 안 적었다.

그 한 줄은 스키마에 들어간다(필드 추가가 아니라 규칙 설명이므로 3개월 규칙에 안 걸린다).
루프는 이 규칙을 지키는 첫 소비자가 된다 — active 모듈을 건드리면 계약 갱신을 같은 커밋에
싣는다.

### 구현하면서 바뀐 것 (2026-09-12)

세 가지가 계획과 다르다. 계획이 틀렸다기보다 돌려 보고서야 답할 수 있던 자리다.

**Agent SDK 를 넣지 않았다.** change 를 루프가 돌리지 않기로 했다 — 루프는 고치는 주체가 아니라
고치는 주체를 계약 앞에 세우는 장치다. 그러자 아래 "어디에 사는가" 의 의존 분리 문제가 통째로
사라진다: 루프의 의존이 0 이라 `optionalDependencies` 도 필요 없고, 회귀 테스트가 키·네트워크
없이 돈다. SDK 는 "읽음을 강제" 가 필요해질 때 붙인다 (`loop/MODULE.md` 미결 첫 줄).

**기준선은 `.git/module-loop/session.json` 에 산다.** "파일로 저장하지 않는다" 를 문자 그대로
지키려면 reconcile 이 HEAD 의 임시 worktree 를 떠서 '변경 전' 을 재현해야 하는데, 큰 트리에서
체크아웃 비용이 크고 무엇보다 **scope 를 건너뛴 변경을 막지 못한다** — 구멍이 안 닫힌다.
대신 세션을 HEAD sha 와 게이트 소스 해시로 봉인했다. 둘 중 하나만 어긋나도 세션을 거부하므로
observations/01 이 적은 실패(규칙이 바뀌자 기준선이 거짓이 됨)가 구조적으로 일어나지 않는다.
경계해야 할 것은 파일이라는 형태가 아니라 **커밋되어 낡는 승인 목록**이었다. 그래서 `.git/` 안이다.

**커밋 저자·메시지** (10절 미결의 답). 저자는 루프가 아니라 리포의 git 사용자다 — 루프는 도구이지
결정한 주체가 아니고, 루프를 저자로 적으면 `git log --author` 가 "누가 정했나" 에 답하지 못한다.
코드와 계약은 커밋 경계가 아니라 메시지가 나눈다: 제목이 코드 변경을, 본문 `계약:` 한 줄이 어느
계약서가 왜 바뀌었는지를 적는다. `게이트:` 줄은 그때 게이트가 무엇이라고 했는지를 남긴다 —
판정이 아니라 증거다. active 계약이 묶음에 있는데 `계약:` 줄이 없으면 커밋하지 않는다.

### 어디에 사는가

`loop/` 로 이 리포 안에 두고 자체 MODULE.md 를 갖는다. 별도 리포로 내보내지 않는 이유는
`--json` 의 출력 모양이 게이트와 루프 사이의 계약이라, 두 리포로 갈라지면 그 계약이
버전 경계를 넘어가기 때문이다.

대신 의존이 섞이면 안 된다. I4 는 "**게이트는** 외부 의존 없이 돈다" 이고, 루프가 SDK 를 쓰면
그 경계가 흐려진다. 지키기로 한 방법은 아래 셋이었고, SDK 를 넣지 않으면서 첫 줄은 쓰이지 않았다:

- 루프의 의존은 `optionalDependencies` — 게이트만 설치하는 리포가 SDK 를 안 받는다
- `package.json` 의 `files` 에 `loop/` 를 넣지 않는다 (지금 화이트리스트 방식이라 기본 제외)
- I4 의 근거는 `module-gate.mjs:9-11` 을 계속 가리킨다. 루프가 무엇을 import 하든 그 세 줄은 안 변한다

예상되는 부작용 하나: `loop/` 가 생기면 이 리포 계약서의 책임 칸이 늘어나고, R4(계약 칸
80줄)가 분할 신호를 낼 수 있다. 그때 쪼개는 게 맞다 — 게이트와 루프는 배포 단위만 같지
소유가 다르다.

2026-09-18 — 둘째 줄을 뒤집었다. 소비 리포가 태그로 설치하면서 루프가 패키지에 실린다 (7절 배포, 10절).

### 첫 소비자는 이 리포다

kod-remastered 가 아니라 module-harness 자신에게 먼저 돌린다. 게이트를 고치는 커밋은
매번 R1(watch 에 `.mjs`)과 R2(계약 섹션)를 건드려서, 루프가 실제로 일해야 하는 변경이
가장 자주 일어나는 곳이다. 실패해도 잃는 게 적다.

---

## 6. 5단계 — 계약 기반 리뷰

almandu 의 8관점 고정 페르소나를 그대로 옮기지 않는다. 그 목록은 코드와 무관하게 정해져
있어서 리뷰어가 무엇을 봐야 하는지를 매번 새로 정당화해야 했다.

대신 **불변식의 태그가 리뷰어를 정한다.** 스키마는 이미 불변식마다 `[테스트|grep|리뷰]`
를 붙이라고 요구한다:

| 태그 | 리뷰어가 하는 일 |
|---|---|
| `[테스트]` | 그 불변식을 검증하는 테스트를 실제로 돌린다. 통과가 증거다 |
| `[grep]` | 근거의 `재현:` 명령을 다시 돌린다. R13 이 건수만 세는 것을 명령 전체로 넓힌다 |
| `[리뷰]` | 판정 수단이 없다고 계약서가 스스로 적은 것. 여기만 에이전트 판단이 필요하다 |

리뷰 범위는 diff 가 아니라 **diff 가 건드린 파일을 근거로 인용하는 불변식 전부**다. 이
목록은 게이트가 R11 에서 이미 계산한다. 리뷰가 새로 만들 자료구조가 없다.

지적은 반드시 불변식 ID 를 인용한다. 인용할 불변식이 없는 지적은 두 가지 중 하나다 —
계약이 비어 있거나(→ 계약을 고칠 일이다), 리뷰어가 취향을 말하는 것이다(→ 버린다).
이 규칙이 리뷰의 소음을 줄이는 유일한 수단이다.

### 구현 (2026-09-12)

`npx module-gate --review` 가 위 표를 낸다. 판정하지 않고 종료 코드는 언제나 0 이다 — `--scope`
와 같은 급이고, 리뷰가 커밋을 막는 자리가 되면 I2 가 흔들린다. 예상대로 새 자료구조는 없었다:
R11 이 걷던 `불변식 → 근거 → changedSet` 순회를 판정 없이 한 번 더 돌린 것이 전부다.

`[리뷰]` 태그 상한(미결)의 답: **규칙으로 만들지 않는다.** `--review` 가 "불변식이 둘 이상인데
전부 `[리뷰]`" 인 계약서를 세어 말하되 막지는 않는다. 이유 둘 — 계약이 빈약한 것은 계약을 고칠
일이지 커밋을 막을 일이 아니고, 규칙으로 만드는 순간 이미 통과하던 소비 리포의 계약서가 갑자기
FAIL 이 된다. 7절 버전 표의 첫 줄이 정확히 그것을 major 라고 부른다.

`[테스트]` 리뷰어가 "그 불변식을 검증하는 테스트" 를 특정하지는 못한다 — 계약서는 근거만 적고
어느 테스트가 그 문장을 지키는지는 적지 않는다. 루프는 리포의 테스트 명령 전체를 돌리고 통과를
증거로 삼는다. 불변식 하나에 테스트 하나를 잇는 표기는 스키마 필드가 늘어나는 일이라 하지 않았다.

---

## 7. 운영 — 패키지가 되면서 새로 생긴 것

### 버전

게이트의 규칙 번호는 공개 계약이다. 소비 리포의 계약서가 `(R14)` 같은 문자열을 직접
인용하고, am-gate.json 의 `_why` 가 규칙 동작을 서술한다.

| 바뀐 것 | 버전 |
|---|---|
| 이미 통과하던 계약서가 FAIL 이 된다 | major |
| 규칙 추가, `--json` 필드 추가, 새 모드 | minor |
| 메시지 문구, 버그 수정, 성능 | patch |
| 패키지에 실리는 표면이 는다 (`files`·bin) | minor |

핵심은 첫 줄이다. 규칙을 하나 조이면 소비 리포의 모든 커밋이 막힐 수 있고, 그건 기능
추가가 아니라 호환성 파괴다. R13 예산을 24 → 64 로 올린 것 같은 완화는 minor 다.

0.x 인 동안은 major 자리가 없으므로 minor 를 그렇게 쓴다. 1.0 은 소비 리포가 둘 이상
생기는 시점에 매긴다 — 하나뿐인 지금은 고정할 게 없다.

0.6.0 부터 루프 CLI 도 이 표의 대상이다 — 명령·플래그를 빼거나 뜻을 바꾸는 것, 종료 코드(0/1/2)의 뜻을 바꾸는 것, `Review:`·`Review-Verdict:` 트레일러의 형식을 바꾸는 것은 첫 줄과 같은 급이다. 소비 리포의 커맨드와 이미 커밋된 트레일러를 읽는 쪽이 깨진다. 패킷(`review-packet.json`)의 모양은 여기 들지 않는다 — 읽는 쪽인 페르소나가 같은 태그에 실려 함께 움직인다.

2026-09-18 — 0.7.0 에서 패키지 이름이 `module-harness` 에서 `almandu-harness` 로 바뀐다. 소비 리포의 설치 자리(`node_modules/module-harness/…`)가 깨지고 이미 통과하던 `(외부: module-harness)` 표지가 R14 FAIL 이 되므로 첫 줄과 같은 급이다 — 0.x 라 minor 로 쓴다. bin 은 `almandu-module-gate`·`almandu-harness-init`·`almandu-module-loop` 가 정식이고 옛 이름 셋은 같은 파일을 가리키는 별칭이다. 별칭을 나중에 빼는 것도 첫 줄과 같은 급이다 — 이미 놓인 훅이 옛 이름을 부른다 (뺄 조건은 10절 미결).

### 배포

`file:../module-harness` 는 같은 머신에서만 산다. 다음은 git 태그다:

```
npm i -D github:shanash/module-harness#v0.6.0
```

레지스트리 공개는 지금 안 한다. 얻는 게 설치 문자열 단축뿐이고, 이름 선점과 공개 범위를
결정해야 하는 비용이 그보다 크다. `package.json` 의 `private: false` 는 그 결정을 미리
내린 것처럼 보이므로 미결로 남긴다.

2026-09-18 — GitHub `shanash/module-harness` 를 비공개로 만들었다. 설치 문자열은 위 그대로이고 설치하는 머신마다 SSH 키가 있어야 한다. 공개 여부는 10절 미결이다. 밀어 올린 태그는 옮기지 않는다 — 고칠 것이 생기면 다음 버전을 찍는다.

2026-09-18 — 0.7.0 에서 패키지 이름은 `almandu-harness` 가 됐지만 GitHub 리포와 로컬 디렉토리는 여전히 `module-harness` 다. npm 은 설치 문자열의 리포 이름과 무관하게 `package.json` 의 `name` 자리에 설치하므로 위 설치 문자열은 그대로 `node_modules/almandu-harness/` 를 만든다. 둘을 바꾸는 것은 따로 하는 사람의 일이다 — GitHub 는 옛 URL 을 새 이름으로 넘겨 주지만, 디렉토리를 바꾸면 소비 리포의 `file:../module-harness` 가 끊기므로 소비 리포가 옮긴 뒤에만 한다 (10절 미결).

### CI

`.github/workflows/ci.yml` 이 이 셋을 돌린다 (2026-09-12):

```
npm test                                      # 85개, ~75초
npx --no-install almandu-module-gate --base origin/main    # PR 이 계약을 어기는지
npx --no-install almandu-module-gate --audit               # 전수 — nightly(03:00 KST)와 수동 실행만
```

`--audit` 를 매 PR 에 걸지 않는 이유는 diff 와 무관하게 전부를 여는 모드라서다. 처음
돌렸을 때 나온 3건 중 하나는 그 작업과 무관하게 전부터 밀려 있던 것이었다 — 그런 것은
PR 을 막을 근거가 아니라 따로 쌓아 두고 볼 목록이다.

### 설치

소비 리포가 손으로 하는 일이 지금 셋이다 — pre-commit 훅 작성, 루트 CLAUDE.md 에 계약
문단 추가, 모듈마다 어댑터 CLAUDE.md 생성. 두 번째 리포를 붙일 때 이걸 자동화한다.

**게이트 바이너리에 넣지 않는다.** `module-gate --init` 로 만들면 판정 도구가 파일을
쓰게 되고 I5 가 흐려진다(`--fix` 는 근거 경로만, `근거:` 있는 줄만 건드리는 예외다).
별도 bin `module-harness-init` 으로 낸다.

구현 (2026-09-12): 셋을 다 놓는다. 예상과 달랐던 것 하나 — **어댑터 문구를 설치 도구 안에 적지
않았다.** 적으면 스키마와 두 판본이 생기고, 그때부터 "바이트 단위로 같게 유지한다" 는 사람이
지키는 약속이 된다. 대신 `MODULE-schema-v1.md` 의 어댑터 블록을 읽어서 쓴다 — harness I6 이고,
설치 도구가 스키마를 읽는 유일한 이유다. 이미 있는 파일은 덮어쓰지 않고, 이미 있는 CLAUDE.md
에는 어댑터를 맨 앞에 얹는다(`@import` 는 위에 있어야 읽힌다). 두 번 돌려도 같은 상태다.

---

## 8. 이 설계가 건드릴 계약

먼저 고쳐야 하는 것을 미리 적어 둔다. 계약을 나중에 맞추면 R2 가 우는 게 아니라 계약서가
거짓인 채로 며칠을 보낸다.

| 언제 | 무엇 |
|---|---|
| 0단계 | 스키마의 `.harness/` 참조 3곳 → 이 리포 MODULE.md 이력 한 줄 |
| 4a | `진입점` 칸에 `--json`·`--scope` 추가. I2·I5 는 안 바뀐다(그게 이 설계의 제약이다) |
| 4b | `책임` 칸에 `loop/` 의 자리. `loop/MODULE.md` 신설. 스키마에 "active 모듈은 계약 갱신을 코드 커밋과 같은 묶음에 싣는다" 한 줄 |
| 5 | 스키마의 태그 절에 리뷰어 배치 규칙 |
| 운영 | README 설치 절, 버전 규칙은 이 문서가 소유 |

이 문서 자신도 계약의 일부가 된다 — `MODULE.md` 책임 칸이 `DESIGN.md` 의 자리를 적는다.
`observations/` 가 "무엇이 일어났나" 를 갖고, 이 파일이 "무엇을 하기로 했나" 를 갖는다.

---

## 9. 버리는 것

almandu 에서 가져오지 않는 것을 명시한다. 나중에 "왜 안 가져왔지" 를 다시 묻지 않기 위해서다.

- **19종 스킬** — scope·change·reconcile·review 넷으로 흡수된다. 파이프라인 단계가 스킬
  개수만큼 있으면 어느 단계가 무엇을 보장하는지 아무도 못 센다
- **`.am/{slug}/` 작업 단위 산출물** — 작업이 끝나면 지식이 코드 경로로 돌아오지 않는
  구조였다. 이 하네스의 전제가 그 반대다
- **8관점 고정 페르소나** — 6절에서 태그 기반으로 대체
- **`MODULE-schema-v0.md`** — 어느 규칙도 읽지 않는다. 0단계에서 지운다. 히스토리에 있다

---

## 10. 미결

- ~~`--scope` 가 조상 계약을 내는가 (4절)~~ — 낸다. 4b 를 다섯 번 돌려 보니 체인이 1~2장이라
  "루트 계약이 매번 실린다" 는 비용이 걱정한 만큼 크지 않았고, 조상을 알아야 `commit` 이
  "계약을 안 연 경로" 를 판정할 수 있다. 부르는 쪽이 앞에서 자를 수 있다는 여지는 그대로 둔다
- 레지스트리 공개 여부와 `private: false`, 그리고 GitHub 리포의 공개 여부 — 지금은 비공개다 (7절 배포)
- `watch` 에 `.md` 가 `MODULE-schema-v1.md` 하나뿐이라 이 문서와 `observations/` 의 유실은
  아무도 안 잡는다. 이 문서가 추적 대상이 되면서 그 미결이 한 칸 더 무거워졌다 —
  `DESIGN.md` 를 `watch` 에 넣으면 게이트 수정마다 이 문서를 고치라고 요구하게 되므로
  넣지 않는다. 대신 낡았는지는 사람이 본다
- `scope` 는 계약 목록을 낼 뿐 그것을 읽었는지는 모른다. 목록과 읽음 사이의 간격이 4b 이후에도
  남은 유일한 습관 의존이다 — 메우려면 루프가 읽는 주체가 되어야 한다 (`loop/MODULE.md` 미결)
- ~~`loop/` 를 언제 `files` 화이트리스트에 넣을지~~ — 0.6.0 에 넣었다 (2026-09-18). 소비 리포가
  `file:` 을 떠나 태그로 설치하는 순간 심링크로 닿던 루프·페르소나가 사라지므로, 한 태그가
  게이트·루프·페르소나를 함께 고정하는 쪽을 골랐다. 루프만 형제 체크아웃에서 부르면 "태그로
  고정" 이 절반만 참이다. 대가로 `module-loop` bin 과 그 CLI 표면이 공개 계약이 됐다 (7절 버전 표)
- `loop` 계약이 `status: draft` 인 채로 공개 표면이 됐다. 인용 전수 검토 뒤 active 로 올릴지 —
  올리면 `loop/` 의 `.mjs` 변경마다 그 계약서가 R1 으로 묶인다
- `--contract` 줄의 거짓을 아무도 잡지 못한다 (observations/03 §5). 게이트는 "계약서를 고쳤나" 를,
  루프는 "무엇을 고쳤다고 적었나" 를 볼 뿐 둘이 같은지는 아무도 안 본다. 지금 남은 가장 큰 구멍이고,
  다음 규칙의 자리일 수 있다
- `--review` 의 "0건" 이 두 가지를 뜻한다 — 리뷰할 것이 없는 변경과 계약이 비어 있는 변경.
  출력이 둘을 구분하지 못한다 (observations/03 §4)
- 옛 bin 별칭(`module-gate`·`module-harness-init`·`module-loop`)을 언제 뺄지 (7절 버전 표) —
  소비 리포의 훅·커맨드·설정 어디에도 옛 이름을 부르는 곳이 없을 때 뺀다. 빼는 것은 첫 줄과
  같은 급의 변경으로 낸다. 설치 도구가 이미 놓은 훅은 덮어쓰지 않으므로 소비 리포가 스스로 옮겨야 그 조건이 찬다
- GitHub 리포(`shanash/module-harness`)와 로컬 디렉토리의 이름을 `almandu-harness` 로 바꿀지 — 사람이
  정할 일이다 (7절 배포). 디렉토리는 소비 리포가 `file:../module-harness` 를 떠난 뒤에만 바꾼다
