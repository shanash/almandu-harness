# DESIGN-review — 5단계 멀티 페르소나 리뷰

> module-harness v0.3.0 위에 얹는 마지막 단계. 이 문서는 4단계 DESIGN 의 5절을 대체한다.
> 원칙: **리뷰는 판정하지 않는다. 규칙이 될 후보를 찾는다.** 판정은 게이트가, 결정은 사람이 한다.

---

## 0. 목적과 비목적

**목적** — 기계가 판정할 수 없는 세 질문에 판단을 붙이고, 그 판단이 반복되면 게이트 규칙으로 내린다.
게이트에서 미결 → 불변식 승격 경로가 있듯, 리뷰 → 관찰 → 규칙 후보 → 게이트 규칙의 승격 경로를 만든다.

**비목적**
- 커밋을 막지 않는다. 리뷰 결과는 기록이고, 막을지는 사람이 정한다
- 코드 품질 일반(스타일, 성능, 중복)을 보지 않는다. 계약과 diff 의 관계만 본다
- 페르소나를 늘리지 않는다. 질문이 늘 때만 페르소나가 는다

**왜 비차단인가** — 리뷰는 LLM 판단이라 비결정론이다. 루프에 SDK 를 넣지 않은 이유와 같다.
같은 diff 에 다른 판정이 나오는 것을 차단 경로에 두면 게이트의 신뢰가 같이 무너진다.

---

## 1. 루프 안의 위치

```
scope → change → reconcile → review → commit
                              ↑ 새로 끼는 단계. reconcile 이 OK 여야 돈다
```

- reconcile 이 FAIL 이면 review 는 돌지 않는다. 기계가 이미 거부한 것에 판단을 붙일 이유가 없다
- review 는 **패킷 생성**(CLI, 결정론)과 **판단**(호출자, 비결정론) 두 부분으로 나뉜다.
  CLI 는 판단을 하지 않는다 — 4단계와 같은 경계
- commit 은 review 결과가 없어도 진행한다. 다만 결과 유무와 verdict 를 커밋 trailer 에 남긴다

---

## 2. 입력 — 리뷰 패킷

`loop review --packet` 이 `.git/module-loop/review-packet.json` 을 만든다. 세션 봉인(HEAD sha + 게이트 해시)을 그대로 상속한다.

```json
{
  "session": { "head": "…", "gate_hash": "…" },
  "scope":   ["engine", "actions"],
  "diff": {
    "code":     ["restored-project/Assets/Scripts/Engine/Types/LeValue.cs"],
    "contract": ["restored-project/Assets/Scripts/Engine/MODULE.md"],
    "owners":   { "…LeValue.cs": "engine" },
    "content":  "<묶음 파일 본문의 sha256>"
  },
  "contract_statement": "I11 신설 — 값 변환 구현은 StringWith 한 곳",
  "contract_diff": "<MODULE.md 의 unified diff, 계약 섹션만>",
  "review_invariants": [
    { "module": "engine", "id": "I3", "text": "선택지 분기는 ChoiceResolver 만 결정한다",
      "evidence": ["…/ChoiceResolver.cs:40"], "touched": true }
  ]
}
```

- `review_invariants` 는 `--review` 가 이미 뽑는 [리뷰] 태그 불변식 중 **diff 가 인용 파일을 건드린 것**만.
  `touched: false` 인 것은 넣지 않는다 — 읽지 않은 코드에 판단을 붙이면 추측이 된다
- `contract_diff` 는 계약 섹션(책임·진입점·의존·불변식)만. 미결·이력은 뺀다 — R2 와 같은 경계
- 패킷은 재생성 가능하고 커밋되지 않는다. 클론과 함께 죽는다
- `diff.content` 는 묶음 파일 전부의 본문(지운 파일·심볼릭 링크·실행 비트 포함) 해시다 (2026-09-17, 0.5.0).
  경로 목록만 담으면 같은 파일 안에서 코드를 통째로 바꿔도 해시가 그대로라, 리뷰 뒤에 고친 코드에 앞선 답이
  묶였다 (`observations/05-review-field.md` 결함 메모 6). 페르소나는 이 필드를 읽지 않는다 — 봉인용이다

---

## 3. 세 질문, 세 페르소나 (계약 대조자 2026-09-16, 범위 감시자 2026-09-17 에 내려 현재 하나)

페르소나는 질문의 이름이다. 성격이나 말투가 아니다.

| 페르소나 | 질문 | 입력 | 답 |
|---|---|---|---|
| ~~**계약 대조자**~~ (내림) | `contract_statement` 가 `contract_diff` 를 정직하게 서술하는가 | 문장 + 계약 diff | 예 / 아니오 / 판단불가 |
| **불변식 판정자** | 각 `review_invariants` 항목이 이 diff 뒤에도 참인가 | 불변식 + 근거 파일 현재 내용 + 코드 diff | 항목마다 참 / 거짓 / 판단불가 |
| ~~**범위 감시자**~~ (내림) | `diff.owners` 의 값이 전부 `scope` 안에 있는가 | scope + owners | 예 / 아니오 |

**계약 대조자**가 첫째인 이유 — 4단계가 남긴 가장 큰 구멍이다. 게이트는 "계약서를 고쳤나"를,
루프는 "무엇을 고쳤다고 적었나"를 보는데 둘이 같은지는 아무도 안 본다. 이것은 기계로 못 잡는다
(문장 의미 비교). 리뷰가 값을 하는 첫 사례가 여기서 나온다.

**범위 감시자**는 기계 판정이 가능할 수 있다. 그래도 리뷰로 시작한다 — "scope 밖 변경이 실제로
얼마나 자주, 어떤 이유로 생기는가"를 관찰한 뒤 규칙으로 내린다. 미리 규칙을 만들면 예외 처리를
상상으로 짓게 된다.

### 계약 대조자 내림 (2026-09-16, kod `0b18de5`)

6절 역방향 규칙("10회 연속 예/참이면 리뷰에서 내린다")이 처음 돌았다. 실전 10회 전부 `예` 였고
비통과는 한 건도 없었다 (`observations/05-review-field.md` 관찰 표). 페르소나 파일
`review/personas/contract-checker.md` 는 남기고 `loop/loop.mjs` 의 `ALL_PERSONAS` 에서
`retired` 로 표시해 활성 목록에서만 뺐다 — 지우면 이미 커밋된 트레일러의 `계약대조=예` 를
나중에 읽는 쪽이 그 이름을 되찾지 못한다.

**내리면서 함께 적는 단서 — 10 이라는 수는 겉보기보다 약하다.** 10회를 계약 diff 의 무게로
가르면 실질 4 · 줄 인용 밀림뿐 2 · **계약 diff 자체가 없음 4** 다. 마지막 4회는 페르소나 파일이
"`contract_diff` 가 빈 문자열이면 … 미결·이력만 손댔다고 말하면 `예`" 로 이미 규정해 둔 경우라
사실상 자동 통과이고, 질문이 값을 했는지에 대해 아무것도 말하지 않는다. 즉 이 내림의 근거는
"10회 연속 통과" 라는 **형식 요건의 충족**이지 "질문이 잡을 것이 없음을 10회 확인했다" 가 아니다.
실질 판정은 4회뿐이었고 그 4회도 전부 `예` 였다.

되돌릴 조건: 계약 문장과 계약 diff 가 어긋난 커밋이 사후에 발견되면 (게이트 R2·R10 이 못 보는
자리다) 이 질문을 다시 올린다. 그때 이 절이 "왜 뺐는지" 의 기록이다.

### 범위 감시자 내림 (2026-09-17, kod `c319b98`)

근거가 둘이고, 계약 대조자 때와 달리 두 번째는 표본 수에 기대지 않는다.

1. **6절 역방향** — 실전 19커밋에서 `예` 14회(시간순 연속), `아니오` 0회. `판단불가` 5회는 전부
   `owners` 가 `null` 인 파일 탓이었다(작업 전 잔재 4, 계약 없는 작업 파일 1 — `eec0991`). 페르소나 파일이
   "범위 이탈이 아니라 계약 부재" 로 규정한 경우다. 위에서 이 질문을 리뷰로 시작한 이유가 "scope 밖 변경이
   얼마나 자주, 어떤 이유로 생기는가" 를 세는 것이었는데, 그 답은 **19커밋에서 0회**다.
2. **질문이 이미 기계 규칙이다** (6절 "규칙이 된 질문은 페르소나에서 뺀다") — 패킷의 `scope` 는 세션이 연
   계약들의 슬러그이고 `owners` 는 파일마다 가장 깊은 계약의 슬러그다. `loop commit` 은 가장 깊은 계약이
   세션 목록에 없는 파일이 묶음에 있으면 종료 코드 2 로 거부한다(loop I2). 소유자가 있는 파일에 대해 둘은
   같은 질문이다. 그래서 이 페르소나의 `아니오` 는 **커밋되지 못할 패킷에서만** 나오고, trailer 에 남는 실전
   검출(10절 조건 1)은 원리상 될 수 없었다. 04 의 P3 가 이 질문을 잡은 것은 프로브가 커밋되지 않았기
   때문이다. 위 문단이 "기계 판정이 가능할 수 있다" 고 적을 때 그 판정은 4b 의 I2 로 이미 서 있었고,
   설계 때 둘을 대조하지 않았다.

`owners` 가 `null` 인 자리는 I2 가 통과시킨다 — 소유 계약이 없으면 열 계약도 없다. 거기서 낸 `판단불가` 는
범위에 대한 답이 아니라 "이 경로에 계약이 없다" 의 재진술이고, 그 사실은 `--scope` 가 이미 `소유 모듈 없음`
으로 낸다.

되돌릴 조건: loop I2 가 약해져 scope 를 지나지 않은 경로가 커밋될 수 있게 되거나, 루프를 거치지 않는
커밋에 리뷰를 태우는 경로가 생기면 이 질문을 다시 올린다.

패킷 스키마(2절)는 줄이지 않았다 — `scope`·`diff.owners`·`contract_statement`·`contract_diff` 를 읽는
활성 페르소나는 이제 없지만, 스키마를 줄이는 것은 이 내림과 별개의 결정이고 되올릴 때 다시 늘리지
않아도 된다. 트레일러는 `Review: 불변식=…(n)` 한 항목이 되고, 물을 불변식이 0개인 패킷은 답이 없어도
`불변식=없음(0)` 과 `Review-Result` 를 남긴다. 그렇게 하지 않으면 19커밋 중 18커밋 꼴로 `Review: none` 이
되어 "리뷰를 건너뛴 커밋" 과 grep 으로 갈리지 않는다(5절).

각 페르소나 프롬프트는 `review/personas/<이름>.md` 한 파일. 질문, 입력 필드, 출력 형식, **답하지 말 것**
네 절로 고정한다. 답하지 말 것 절이 핵심이다 — 계약 대조자가 코드 품질을 말하기 시작하면 관찰이 오염된다.

---

## 4. 출력 — verdict

`.git/module-loop/review-result.json`:

```json
{
  "packet_hash": "…",
  "verdicts": [
    { "persona": "계약 대조자", "answer": "아니오",
      "reason": "문장은 '근거 정정'이라 하나 diff 는 I3 문장 자체를 바꿈",
      "evidence": ["MODULE.md:31"] },
    { "persona": "불변식 판정자", "invariant": "engine/I3", "answer": "참",
      "reason": "ChoiceResolver 외 호출 없음", "evidence": [] },
    { "persona": "범위 감시자", "answer": "예", "reason": "", "evidence": [] }
  ],
  "summary": "막음 후보 1 / 통과 2 / 판단불가 0"
}
```

규칙:
- `reason` 은 한 문장. `evidence` 는 파일:라인. 없으면 빈 배열 — **빈 배열은 허용되지만 `판단불가`와 함께여야 한다**.
  "아니오"에 근거가 없으면 패킷 오류로 취급한다
- `answer` 는 셋 중 하나. 자유 문장 금지 — 관찰 대조를 기계로 하기 위해서
- `packet_hash` 가 현재 패킷과 다르면 result 는 무효. 봉인 규칙과 같다.
  "현재 패킷" 은 파일로 쓴 패킷이 아니라 **커밋 시점의 묶음으로 다시 만든 패킷**이다 (0.5.0) — 리뷰를 받은 뒤
  잔재를 빼거나 코드를 고치면 파일로 쓴 패킷은 그대로 남아 낡은 답을 가리킨다
- `superseded` — 패킷이 바뀌어 버린 답과 같은 패킷에서 덮어쓴 답 중 **통과가 아닌 것**만 `packet_hash` 와 함께
  옮겨 둔다 (0.5.0). "거짓 → 수정 → 참" 의 앞쪽이 6절 승격 경로의 원료라 버리지 않는다. 통과 답은 옮기지 않는다

---

## 5. 실행 메커니즘

CLI 는 판단하지 않으므로 페르소나 실행은 **호출자**가 한다. 두 경로:

**A. Claude Code 세션** — `/module-review` 커맨드가 패킷을 읽고 페르소나 셋을 서브에이전트로 띄운다.
각 서브에이전트는 자기 페르소나 파일 + 패킷만 받는다. 세션 컨텍스트를 상속하지 않는다 — change 를
한 세션이 자기 변경을 리뷰하면 판정이 아니라 변명이 된다. 결과를 `review-result.json` 에 쓴다.

**B. 사람** — 패킷을 읽고 `loop review --answer <persona> <answer> --reason "…"` 로 직접 쓴다.
리뷰가 LLM 전용이 아니어야 CI 없는 환경과 Claude Code 밖에서도 성립한다.

`loop commit` 은 result 가 있으면 trailer 로 남긴다:

```
Review: 계약대조=아니오 불변식=참(1) 범위=예
Review-Result: <packet_hash 앞 8자리>
```

result 가 없으면 `Review: none`. **없어도 커밋된다.** 다만 trailer 가 없다는 사실이 기록에 남아,
나중에 "리뷰를 건너뛴 커밋"을 grep 으로 셀 수 있다.

**추가 (2026-09-17, 0.5.0)** — 두 결함 메모(05 의 6·1)를 이 절에서 닫는다.

- `commit` 은 파일로 쓴 패킷을 믿지 않고 지금 묶음으로 패킷을 다시 만들어 해시를 대조한다. 다르면 답을 싣지
  않고 `Review: none` 으로 남기며, 빠진·들어온 파일이나 내용 변화를 stderr 로 알린다. 거부하지 않는다 — 0절의
  비차단 원칙 그대로이고, 트레일러가 커밋되는 트리를 가리키지 않는 것보다 `none` 이 정직하다
- 통과가 아닌 답은 이유와 근거까지 커밋에 남는다. 답 하나에 한 줄이다:

  ```
  Review-Verdict: <packet_hash 앞 8자리> 불변식 engine/I3=거짓 | <reason> | <evidence …>
  ```

  `superseded` 와 지금 result 의 답을 모두 싣는다. 결과가 이 커밋의 패킷에 묶이지 않아 `Review: none` 이어도
  싣는다 — 줄마다 패킷 해시가 있어 어느 트리에 대한 답인지 갈린다. 자리를 커밋 메시지로 정한 이유는 소비
  리포의 루프가 하네스의 `observations/` 에 쓸 수 없고, 작업 트리에 쓰면 loop I5 가 깨지고, `.git/` 안에 두면
  클론되지 않기 때문이다. 커밋과 함께 클론되는 자리는 메시지뿐이다

---

## 6. 승격 경로 — 리뷰가 규칙이 되는 조건

```
verdict(아니오/거짓) → observations/NN 에 기록 → 같은 이유 3회 → 규칙 후보 → 게이트 규칙 (R14~)
```

- **3회**가 기준이다. 1회는 사고, 2회는 우연일 수 있다. 3회면 패턴이고, 패턴이면 기계가 볼 수 있어야 한다
- 규칙 후보는 `DESIGN` 미결에 `- 규칙 후보: <이유> (관찰 3건: 04 §a, 05 §b, 05 §d)` 형식으로 적는다.
  미결 3상태와 같은 규율 — 후보가 규칙이 되면 줄을 삭제하고 게이트 표에 R 번호가 생긴다
- 규칙이 된 질문은 페르소나에서 뺀다. 기계가 보는 것을 사람이 또 보면 관찰이 이중 계산된다

역방향도 있다. 어떤 페르소나가 10회 연속 "예/참"만 답하면 그 질문은 리뷰 대상에서 내린다.
잡는 게 없는 리뷰는 비용만 남는다. 내린 기록은 DESIGN 에 남긴다 — 왜 뺐는지 없으면 다시 넣게 된다.

---

## 7. 프로브 — 구조 검증

R11 을 테스트 파일에 주석 한 줄로 검증했듯, 리뷰도 일부러 나쁜 커밋으로 검증한다.
셋 다 kod-remastered 에서 돌린다 — 하네스 리포는 계약이 얇아 [리뷰] 불변식이 거의 없다.

| 프로브 | 만드는 법 | 잡아야 할 페르소나 |
|---|---|---|
| P1 거짓 서술 | `--contract "I3 근거 경로 정정"` 이라 적고 실제로는 I3 문장을 바꾼다 | 계약 대조자 → 아니오 |
| P2 조용한 위반 | [리뷰] 불변식을 깨는 코드를 넣고 MODULE.md 는 안 건드린다 (R1 은 울겠지만 계약서를 형식적으로만 고쳐 넘긴다) | 불변식 판정자 → 거짓 |
| P3 범위 이탈 | scope 에 engine 만 넣고 actions 파일을 고친다 | 범위 감시자 → 아니오 |

각 프로브는 `git checkout --` 으로 되돌린다. 결과는 observations/04 에 그대로 붙인다.
**셋 중 하나라도 통과시키면 그것이 04 의 첫 항목이다.** 다 잡으면 구조가 선 것이고 실전으로 간다.

프로브에 더해 **정상 커밋 3건**도 돌린다. 리뷰가 정상 커밋에 "아니오"를 내면 오탐이고, 오탐률이
프로브 검출률만큼 중요하다. R10 이 "완료"라는 단어에 헛울었던 것을 기억할 것.

---

## 8. 관찰 기록 — observations/04-review-observation.md

```markdown
# 04 리뷰 관찰 — <날짜>

## 프로브
| 프로브 | 기대 | 실제 | 판정 |
|---|---|---|---|

## 정상 커밋
| 커밋 | 페르소나 | answer | 오탐 여부 |

## 규칙 후보 (같은 이유 묶음)
- <이유> — 관찰 N건: …

## 페르소나 결함
- <페르소나> 가 <질문 밖의 것> 을 말함 → 프롬프트 "답하지 말 것" 절 보강

## 결론
```

게이트 관찰(02)이 "규칙 부족 / 계약 부족"으로 갈랐듯, 리뷰 관찰은 **"검출 / 오탐 / 페르소나 결함"** 셋으로 가른다.
섞으면 셋 다 안 고쳐진다.

**결정 (2026-09-13) — 05 의 카운터 모집단은 커밋 trailer 다.** 연속 통과와 오탐률을 세는 대상은
`Review:` trailer 가 붙은 커밋뿐이고, 되돌린 프로브는 세지 않는다. 04 가 적은 "범위 감시자 연속 통과 7회" 는
05 로 승계하지 않는다 — 그 7 은 프로브 실행과 정상 커밋을 섞은 **누계**이고, P3 의 `아니오` 가 두 회차 모두
가운데 있어 연속도 아니며, 프로브는 커밋되지 않아 trailer 로 특정할 수단이 아예 없다. 04 는 원본 관찰이므로
고치지 않는다. 05 는 재현 가능한 모집단 하나 위에서 실측치로 시작한다 (10절 판단이 근거를 갖는 유일한 조건).

---

## 9. 구현 순서 — 세션 5개

각 세션은 자기완결 프롬프트로 돌린다. 4단계와 같은 방식.

**상태 (2026-09-17)** — 5a~5e 끝남. 5e 는 kod-remastered 실제 작업 커밋 19건으로 닫았다
(`observations/05-review-field.md`, 분모는 그 문서의 "셈의 규칙"). 종료 조건은 ②·③ 충족, ① 미충족이고
그 경우를 어떻게 읽는지는 10절 끝의 결정에 있다. 활성 페르소나는 불변식 판정자 하나다(3절).

**5a — 패킷** (module-harness)
`loop review --packet` 구현. 2절 스키마 그대로. 테스트: 패킷이 결정론적인가(같은 트리 → 같은 해시),
`touched: false` 가 빠지는가, 계약 diff 에 미결·이력이 안 들어가는가.
종료 조건: 테스트 통과, 봉인 위반 시 거부.

**5b — 페르소나와 결과** (module-harness)
`review/personas/*.md` 셋, `loop review --answer`, `review-result.json` 스키마 검증(4절 규칙),
commit trailer. `/module-review` 커맨드 초안. 테스트: "아니오"에 evidence 없으면 거부, packet_hash 불일치 시 무효.
종료 조건: 사람 경로(B)로 verdict 하나를 손으로 써서 커밋 trailer 에 남는 것을 확인.

**5c — 설치와 프로브** (kod-remastered)
v0.4.0 태그 후 `github:shanash/module-harness#v0.4.0` 으로 갱신. 12장 게이트 통과 확인.
7절 프로브 3종 + 정상 커밋 3건. observations/04 작성.
종료 조건: 04 에 프로브 결과표와 오탐 표가 채워짐.

**5d — 페르소나 고정** (module-harness)
04 의 "페르소나 결함"으로 프롬프트 보강. 답하지 말 것 절 확정. v0.4.1.
종료 조건: 같은 프로브 재실행에서 결함 항목이 사라짐.

**5e — 실전 관찰** (kod-remastered, 기간)
실제 작업 커밋 10건 이상에 리뷰를 태운다. 규칙 후보가 나오면 6절 경로로.
종료 조건 — 아래 10절.

---

## 10. 종료 조건

다음 셋이 모두 관찰됐을 때 5단계는 끝난다.

1. 리뷰가 실제 작업 커밋에서 "아니오/거짓"을 낸 사례가 있고, 그것이 사고였음을 사람이 확인했다
   (프로브가 아닌 실전 검출 1건 이상)
2. 규칙 후보가 하나 이상 생겼거나, 어떤 질문이 10회 연속 통과라 리뷰에서 내려졌다
   (승격 경로가 어느 방향으로든 한 번 돌았다)
3. 오탐이 정상 커밋 10건 중 1건 이하다

셋 다 없으면 5단계는 "구조는 섰지만 값은 못 했다"이고, 그것도 결론이다. DESIGN 에 그렇게 적고
리뷰를 비활성으로 내린다. 값 없는 단계를 켜 두는 것이 almandu 가 낡은 방식이었다.

**결정 (2026-09-17) — 셋 중 둘이 찬 경우.** 위 두 문단은 "셋 다" 와 "셋 다 없음" 만 정했다. 5e 는 ②·③ 이 차고
① 이 0 인 채로 19커밋에 닿았고, 이 경우를 **5단계 종료**로 읽는다. 결론은 "구조는 섰고 오탐 없이 돌았지만,
실전 검출로 값을 입증하지는 못했다" 다. ① 을 충족으로 고쳐 읽지 않는다 — ✗ 는 ✗ 로 남는다.
리뷰를 비활성으로 내리지도 않는다 — 셋 다 없는 경우가 아니고, 남은 불변식 판정자는 대상이 없으면 돌지 않아
켜 두는 비용이 없다. 기다리지 않고 닫는 근거는 `observations/05-review-field.md` 결론 절에 있다.

---

## 11. 하지 말 것

- 페르소나에 성격을 주지 않는다. "꼼꼼한 시니어" 같은 수식은 질문을 흐린다
- 페르소나끼리 토론시키지 않는다. 답 셋을 병렬로 받는다. 합의는 관찰 대조를 불가능하게 만든다
- change 를 한 세션이 리뷰하지 않는다 (5절)
- 리뷰 결과로 커밋을 막지 않는다. 막고 싶은 것이 생기면 그것은 규칙 후보다 (6절)
- 질문을 늘리기 전에 기존 질문의 관찰이 10건 이상인지 본다

## 12. 재검토 조건

- 리뷰가 차단이 되어야 한다는 요구가 생기면: 그 요구가 가리키는 것이 규칙으로 내려갈 수 있는지 먼저 본다.
  내려갈 수 없는 것만이 차단 리뷰의 근거가 되며, 그때는 이 문서 0절을 다시 쓴다
- 페르소나 실행에 SDK 가 필요해지면: 4단계 DESIGN 5절의 재검토 조건과 같이 판단한다

## 13. 미결

- ~~결정: 비통과 verdict 를 커밋 후에도 보존한다~~ — 2026-09-17 0.5.0 에서 이행. `Review-Verdict:` 트레일러로 남긴다 (5절 추가, loop I11)
- ~~결정: 계약 대조자의 "판단불가" 비율이 높을 때 `contract_statement` 형식을 구조화할지~~ — 2026-09-16 종결. 실전 10회에서 `판단불가` 가 0건이었고 질문 자체를 내렸다 (3절 내림 기록). 다시 올릴 일이 생기면 이 줄도 함께 되살린다
- `review_invariants` 의 `touched` 를 파일 단위로 볼지 라인 범위까지 볼지 미정. 파일 단위로 시작
- 서브에이전트가 근거 파일을 읽는 범위를 패킷이 제한할지, 자유롭게 읽게 둘지 미정. 자유로 시작하고 04 에서 판단
