# MODULE.md 스키마 v1

> 한 디렉토리(=모듈)가 지켜야 할 계약. 사람과 에이전트가 같이 읽고, 게이트가 diff 로 검사한다.
> 원칙: **작업 로그가 아니라 계약서다.** "무엇을 했는지"는 `.am/` 에, "무엇이어야 하는지"만 여기에.
> (하네스의 `observations/` 는 셋째 것이다 — 작업 로그도 계약도 아닌, 게이트를 돌려 본 관찰)
>
> v0 → v1: kod-remastered 에서 모듈 2개(restored-project, engine)를 세우고 실제 변경 1건을
> 통과시키며 드러난 것만 반영했다. 추측으로 추가한 규칙은 없다.

---

## 템플릿

```markdown
---
module: engine                              # 트리 전체에서 유일한 slug
path: restored-project/Assets/Scripts/Engine # 리포 루트 기준. 게이트가 diff 매칭에 사용
schema: 1
status: draft                               # draft | active | frozen | deprecated
watch: .cs,.asmdef                          # 선택. R1 이 "소스"로 볼 확장자 또는 파일명
---

## 책임
<!-- 소유하는 것 1~3문장 + "하지 않는다" 한 문장 -->

## 진입점
<!-- 안내판. 공개 API 목록이 아니다. 보통 3개 안팎이지만 숫자가 기준은 아니다 -->
- `LeEngine.Run(scriptId)` — Runtime/LeEngine.cs

## 의존
### in (이 모듈이 쓰는 것)
- [[data]] — 변수 저장소 조회
### out (이 모듈을 쓰는 것)
- [[tools]] — 명령 테이블 미러 검증

## 불변식
- I1. `LeScriptContainer.SetLoader(null)` 은 `ArgumentNullException` 을 던진다 (근거: restored-project/Assets/Scripts/Engine/Runtime/LeScriptContainer.cs:171) [grep]
- I5. (폐기 2026-09-10 — [[engine]] I1 로 이관. 규칙은 살아 있고 소유자만 옮겼다)

## 미결
- 디렉토리 밖 133개 호출부를 `~With(conv)` 로 옮길지 미정
- 결정: 미설정 변수 확인 전용 문법을 언어에 추가한다 (예정)

## 이력
- 2026-09-10 I9 검증 수단 [grep]→[테스트] (run 0043)
```

---

## 필드별 규칙

| 필드 | 들어가는 것 | 안 들어가는 것 | 게이트 |
|---|---|---|---|
| 책임 | 소유 범위, 명시적 비소유 | 구현 설명 | — |
| 진입점 | 모듈을 **동작시키는 시작점** | 공개 API 전부, 데이터 타입, 값 객체, 내부 헬퍼 | — (사람이 큐레이션) |
| 의존 in/out | 다른 모듈 slug + 한 줄 이유 | 엔진·표준 라이브러리 | R3 대칭·간선 인용 일치 |
| 불변식 | 참/거짓 판정 가능한 **한 문장** | 희망사항, 스타일 | R5 근거·태그, R6 소유, R8 중복, R11 근거 추적 |
| 미결 | 질문, 또는 결정됐으나 미구현 | 완료 항목, 작업 로그, 버그, TODO | R7 분할, R10 완료 표기 |
| 이력 | 계약 변경만 | 코드 변경 전부 | R2 |

**인용 경로는 어느 섹션이든 리포 루트 기준이다** — 진입점·의존·불변식 근거가 같은 기준을 쓴다. 게이트가 검사하는 것은 근거 줄뿐이므로(R12) 나머지 두 섹션은 사람이 지킨다.

**줄번호는 선택이다.** `파일.cs` 단독과 `파일.py::테스트명` 도 인용으로 세며, R6·R8·R11·R12 가 모두 본다.
줄번호 없는 인용은 파일 전체가 하나의 인용 지점이다 — R8 은 `파일.cs` 와 `파일.cs:10` 을 다른 지점으로 본다.
인용으로 세는 것은 소문자 확장자를 가진 토큰과, **확장자가 없지만 리포 루트 기준으로 실재하는 파일**(`tools/git-hooks/pre-commit`)이다. 후자는 경로 조각이 둘 이상이어야 하고 디렉토리는 떨어진다 — 확장자가 없으면 파일 인용이라는 표시가 없으므로 해석 실패는 R12 로 고발하지 않고 조용히 버린다. `LeAction.MakerDictionaryInit` 같은 멤버 표기와 `KoD/Addressables/Setup Infra` 같은 메뉴 경로는 파일이 아니다.

### 진입점 (v1 에서 정의 교체)

이 모듈을 처음 읽는 사람이 **어디서부터 읽어야 하는지** 알려주는 안내판이다. 공개 API 목록이 아니다.

- 디렉토리 밖에서 널리 참조되는 데이터 타입·값 객체는 진입점이 **아니다**. `의존` 이나 불변식으로 표현한다.
  (근거 사례: `LeValue` 는 디렉토리 밖 133개 파일이 참조하지만 진입점이 아니다. `LeTextInterpolator` 도 마찬가지)
- 판단 기준은 참조 수가 아니라 "이것을 부르면 모듈이 동작을 시작하는가".
- v0 표에 있던 "3개 초과 시 경고" 게이트는 **삭제**했다. 구현된 적 없고, 큐레이션은 기계 판정 대상이 아니다.

### 미결 (v1 에서 3상태로 명문화)

미결 항목은 다음 셋 중 하나다. 넷째는 없다.

1. **질문** — 아직 정하지 않은 것. 그대로 둔다
2. **결정됨, 미구현** — `- 결정: <무엇을 한다> (예정)`. 줄은 반드시 `결정:` 으로 시작하고 `(예정)` 으로 끝난다.
   날짜는 적지 않는다 — 그건 이력이 갖는 정보다. 구현되면 이 줄을 **삭제**하고 불변식을 신설한다
3. **완료** — 존재하지 않는다. 삭제된 상태다

"완료"라고 적힌 줄이 남아 있으면 승격 실패 신호다 (R10 이 운다).
승격 = 미결 줄 삭제 + 불변식 신설 + 이력 한 줄. 셋이 한 커밋에 있어야 한다.

**미결 → 불변식 승격 조건: 검증 수단이 존재할 것.** 근거 파일:라인을 적을 수 없고
[테스트|grep|리뷰] 중 하나로 판정할 수 없으면 아직 불변식이 아니다.

### 불변식 ID

- 한 번 붙인 ID 는 **재배열·재사용하지 않는다**. 번호에 빈칸이 생기는 것이 정상이다
- 폐기는 삭제가 아니라 묘비: `- I5. (폐기 <날짜> — 사유/이관처)`
- 묘비는 규칙이 아니므로 근거·검증 수단을 요구하지 않는다 (R5 면제)
- 근거의 파일 경로는 **리포 루트 기준**으로 적는다. 게이트가 소유 모듈을 찾는 기준이다

---

## 모듈 관계

- **부모-자식은 링크로 표현하지 않는다.** 디렉토리 포함 관계로 정의된다.
  루트 모듈과 하위 모듈 사이에 in/out 을 적지 않는다
- **순환 의존은 계약에 그대로 적는다.** 게이트는 순환을 금지하지 않고 R3 대칭과 인용 일치만 본다 — 양쪽에 적으면 조용해진다.
  대칭을 맞추려고 한쪽을 비우면 계약서가 사실을 담지 못한다. 순환을 끊을지는 설계 판단이고 `미결` 에 남긴다
- **in/out 한 줄 이유는 누가 누구를 부르는지 주어가 드러나게 쓴다.** 양쪽 문장이 같은 방향을 가리켜야 한다.
  게이트는 대칭과 인용만 보므로 방향이 뒤집혀도 조용하다 — 사람이 읽는 방향이 틀린 계약서는 실패다.
  양쪽이 같은 파일을 인용하면 줄 표기가 같아야 한다 (R3). 한쪽만 인용한 파일과 줄번호 없는 인용은 묻지 않는다 —
  두 문장은 같은 간선을 다른 각도에서 적는다. 어긋남은 간선 양 끝 중 엄한 쪽 `status` 로 운다
- 변경 파일은 **가장 깊은 모듈**이 소유한다
- 조상 모듈은 공용 테스트·설정의 보관처로 간주하여 R6 에서 면제된다
- 같은 규칙이 두 모듈에 있으면 **깊은 쪽이 소유**하고 얕은 쪽에 묘비를 남긴다
- **하위 모듈이 생기면 상위 `책임` 칸에서 그 범위를 빼고 `[[하위모듈]] 이 소유한다` 로 바꾼다.**
  와일드카드(`Foo.*`)로 범위를 적지 않는다 — 하위 모듈이 생길 때마다 거짓이 된다
- **규칙을 쪼개면서까지 소유권을 내리지 않는다.** 한 문장이 두 모듈의 심볼을 묶어야 성립하는 규칙(미러·대칭)은
  공통 조상이 온전히 갖는다. 반쪽만 내려보내면 판정 불가능한 문장 둘이 남는다
  (근거 사례: restored-project I9 는 `LeActionUtility`([[actions]])와 `LeActionKeys`(ScriptBinary)를 한 문장으로 묶는다)

### CLAUDE.md 어댑터

MODULE.md 가 있는 디렉토리에는 CLAUDE.md 가 있어야 한다 (R9). 어댑터는 재귀하지 않으므로
모듈마다 필요하다. 내용은 이 두 줄 + 프로젝트 고유 규칙:

```markdown
@MODULE.md

이 디렉토리의 계약은 MODULE.md 에 있다. 불변식을 깨는 변경은 먼저 MODULE.md 를 고치고 이력을 남긴다.
하위 디렉토리에 MODULE.md 가 있으면 그쪽이 더 깊은 소유자다. 그 파일을 함께 읽어라.
```

불변식 ID 를 CLAUDE.md 에 나열하지 않는다 — 낡는다.

---

## 게이트 규칙 (module-gate.mjs)

`status: active` 는 FAIL, 그 외는 WARN 으로 강등된다.
**예외는 R3 의 모듈 후보 경고 하나뿐이다** — 상대가 아직 모듈이 아니라는 안내이지 이 계약의 결함이 아니고, 미결에 적으면 침묵하는 승인 경로가 이미 있다. 그래서 언제나 WARN 이다.

| 규칙 | 내용 |
|---|---|
| R0 | frontmatter `module`/`path` 존재, `path` 가 실제 위치와 일치 |
| R1 | 소유 소스 변경 ⇒ MODULE.md 변경. `watch` 항목은 확장자(`.cs`) 또는 경로 꼬리(`git-hooks/pre-commit`, 경로 조각 경계에서 맞음) |
| R2 | 계약 섹션(책임·진입점·의존·불변식) 변경 ⇒ 이력 항목 추가 |
| R3 | 의존 in/out 대칭. 상대 MODULE.md 없으면 모듈 후보 경고 — 미결이 이미 모듈 후보로 적었으면 침묵. 간선 양쪽이 같은 파일을 인용하면 줄 표기도 같아야 함 |
| R4 | 계약 칸(책임·진입점·의존·불변식) 80줄 초과 ⇒ 분할 후보. 미결·이력은 세지 않는다 |
| R5 | 불변식마다 근거 + [테스트\|grep\|리뷰] 태그 (묘비 면제) |
| R6 | 근거가 다른 모듈 소유이면 in/out 중 한쪽에 그 모듈이 있어야 함 (부모-자식은 양방향 면제) |
| R7 | "분할 후보: X" 가 가리키는 곳에 이미 MODULE.md 존재 ⇒ 정리 필요 |
| R8 | 같은 파일:라인을 두 모듈이 근거로 인용 ⇒ 중복 계약 |
| R9 | MODULE.md 있는데 CLAUDE.md 에 `@MODULE.md` 없음 |
| R10 | 미결에 완료 표기가 남아 있음 ⇒ 승격 실패 |
| R11 | (a) 불변식이 근거로 인용한 파일이 변경됐는데 그 MODULE.md 미변경 (소유 무관) (b) 인용 줄 **위쪽**에 diff hunk 가 있어 줄이 밀림 — MODULE.md 가 바뀌었든 아니든 묻는다 |
| R12 | 근거 경로가 리포 루트 기준이 아님(`--fix` 로 자동 정정), 또는 어디로도 해석되지 않음 |
| R13 | 근거의 `재현:` 건수 주장을 게이트가 다시 셈. 관용을 적은 불변식만 — 옵트인 |
| R14 | 리포 밖 모듈 인용 — `in` 쪽에만 `(외부: <패키지명>)`, 설치 목록으로 검증. `out` 은 경계를 넘지 못하고 근거는 `node_modules/` 안을 가리키지 못한다 |

R4 가 미결·이력을 빼고 세는 이유는 그 둘이 줄지 않는 칸이기 때문이다 — 이력은 append-only 이고
미결은 답이 나올 때까지 남는다. 함께 세면 R2 가 늘린 줄을 R4 가 벌하고, 오래된 계약서는 고칠 때마다
책임·불변식을 깎게 된다 (engine 이 2026-09-12 에 그 상태에 처음 닿았다). R4 가 묻는 것은 파일 크기가
아니라 "이 디렉토리가 약속한 것이 많은가" 다.

R11 이 R1 의 사각지대를 덮는다: 검증 수단이 모듈 밖(공용 테스트 디렉토리)에 있을 때,
그 테스트가 약화돼도 R1 은 소유 모듈만 보므로 울지 않는다.

R11(b) 는 다른 질문이다 — "근거 파일이 바뀌었나" 가 아니라 "인용이 아직 그 줄을 가리키나".
계약서를 이미 손댔으면 사람이 봤을 것이라는 (a) 의 가정이 깨진 자리다: 근거 파일이 밀리는 동안
같은 커밋이 **다른 불변식 때문에** 계약서를 건드리면 (a) 는 통째로 건너뛴다.
인용 줄을 이번에 같이 옮겼으면 (b) 는 묻지 않는다 — 그게 정답이고, 다시 물으면 고칠 길이 없다.
줄번호 없는 인용은 줄에 대해 아무 주장도 하지 않으므로 (b) 의 대상이 아니다.

R11(b) 가 못 보는 것은 **여러 커밋에 걸쳐 조금씩 밀린 인용**이다 — 각 커밋에서 0 줄이라 조용하다.
그 전수 감사는 규칙이 아니라 모드다: `npx module-gate --audit` 는 diff 를 아예 보지 않고
"지금 그 줄에 무엇이 있나" 만 묻는다. 파일 끝을 넘었거나 인용 범위가 통째로 빈 줄·중괄호뿐이면
그 인용은 아무것도 주장하지 못한다고 답한다. 주석 줄은 내용으로 센다 — 여러 계약서가 "HARD INVARIANT"
주석을 근거로 인용한다. 이 모드는 커밋 경로에 걸려 있지 않고(평소 판정에 섞이지 않는다) 사람이 부른다.
문장이 맞는지는 판정하지 않는다 — 밀린 것을 가리켜 줄 뿐이고, 그 판단은 사람이 한다.

### 리포 밖 모듈 (R14)

하네스가 npm 패키지로 설치되면서 계약서가 이 리포에 없는 모듈을 인용할 자리가 생겼다. 필드를
더하지 않고 의존 줄 안에서 푼다:

```
- [[harness]] — pre-commit 이 게이트를 돌린다 (외부: module-harness, 근거: tools/git-hooks/pre-commit:95-106)
```

- **`in` 쪽에만 적는다.** `out` 은 경계를 넘지 못한다 — 패키지는 자기를 쓰는 리포를 셀 수 없고,
  셀 수 있다고 적는 순간 그 줄은 거짓이 된다. 쓰는 쪽이 자기 `in` 에 적고, 라이브러리의 `out` 은
  "없음 — 쓰는 리포가 자기 in 에 적는다" 가 된다. 대칭 검사(R3)도 이 간선에는 서지 않는다
- **검증 수단은 설치 목록이다.** `외부:` 가 가리키는 이름이 `package.json` 의 의존이거나
  `node_modules/` 에 실재해야 한다. 그것이 이 줄이 반증 가능한 유일한 지점이다
- **근거는 리포 안에 있어야 한다.** `node_modules/` 안의 파일을 인용하면 R14 가 운다 —
  그 경로는 gitignore 되어 R11 의 diff 에 영원히 나타나지 않으므로, 밀려도 썩어도 아무도 묻지
  않는다. 외부 모듈의 약속을 말해야 하면 그쪽 계약의 불변식 ID 로 말한다 (예: "harness I2")

---

### 건수 주장의 재현 관용 (R13)

`[grep]` 태그는 "세면 판정된다" 는 표시일 뿐 게이트가 그 grep 을 돌리지는 않았다. 건수를 적은
불변식은 계약서를 **다른 이유로** 고치는 순간 조용히 거짓이 될 수 있다 (actions I1 이 40 에서
42 로 틀어졌고 사람이 세서 고쳤다). 근거 안에 이 한 줄을 적으면 게이트가 대신 센다:

```
(근거: <경로들>, 재현: [<범위> 에서 ]`<고정 문자열>` <N>건)
```

- **옵트인이다.** 이 관용을 적은 불변식만 검사한다. 기존 `[grep]` 불변식 전부를 한꺼번에
  대상으로 만들면 계약서 여러 장이 동시에 빨개지고, 그건 규칙이 일한 게 아니라 소음이다
- **고정 문자열**이다 (`git grep -F`). 정규식은 받지 않는다 — 이스케이프 규칙이 계약서마다
  달라지는 것보다 못 적는 편이 낫다. 정규식이 필요해 보이면 대개 범위가 넓은 것이다:
  data I11 의 `new Le*SaveHelper` 는 범위를 파일 하나·디렉토리 하나로 좁히자 `= new Le`·`SaveHelper(`
  두 고정 문자열로 같은 집합이 세어졌다 (2026-09-12, 두 집합이 40건으로 일치함을 확인)
- **범위**는 리포 루트 기준 경로 하나(파일 또는 디렉토리)다. 안 적으면 **모듈 디렉토리**이고,
  그것도 없는 루트 모듈은 반드시 적어야 한다. 리포 전체를 훑는 형태는 없다
- **MODULE.md 는 세지 않는다.** 패턴이 계약서 안에 문자열로 적혀 있어 모든 주장이 제 발에 걸린다
- **한 실행에 64개**까지 센다. 넘으면 넘은 지점부터 세지 않고 그 사실을 경고한다.
  비용은 건수가 아니라 범위 폭이다 — 2026-09-12 실측으로 모듈 디렉토리 범위는 25회 0.19s,
  `restored-project/Assets` 트리 범위는 25회 3.2s 다. 그러니 예산이 아니라 위의 "범위는 경로 하나"
  규칙이 비용을 통제한다. 트리 전체를 범위로 적는 주장은 한두 개까지만 두는 것이 좋다
- `0건` 도 주장이다 — "이 디렉토리에 `: MonoBehaviour` 는 없다" 는 가장 잘 썩는 부류다

---

## 미결

- 없음. 스키마가 자기 사각지대로 적어 둔 것은 2026-09-12 에 전부 닫혔다 — 확장자 없는 근거 인용, R11(b) 의 전수 감사(`--audit`), R13 의 정규식 주장(범위를 좁혀 고정 문자열로 셌다), R13 예산(실측 후 64). 하네스 자신에 대한 열린 질문은 module-harness 의 `MODULE.md` 미결이 갖는다 — 계약서가 있으면 질문도 그쪽 소유다

---

## 3개월간 지킬 것

- 필드 추가 금지. 필요하면 `미결` 에 "스키마에 X 필드 필요?" 로 적고 기다린다
- 계약 칸은 80줄 이내. 넘으면 모듈을 쪼갤 신호 — 미결·이력은 그 수에 들지 않는다
- 엔진 특화 내용은 불변식 문장 안에만 등장하고 스키마에는 등장하지 않는다
- 구현되지 않은 게이트를 스키마 표에 적지 않는다 (v0 의 "3개 초과 경고" 사고)
