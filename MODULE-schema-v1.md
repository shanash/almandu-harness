# MODULE.md 스키마 v1

> 한 디렉토리(=모듈)가 지켜야 할 계약. 사람과 에이전트가 같이 읽고, 게이트가 diff 로 검사한다.
> 원칙: **작업 로그가 아니라 계약서다.** "무엇을 했는지"는 `.am/` 에, "무엇이어야 하는지"만 여기에.
> (`.harness/observations/` 는 셋째 것이다 — 작업 로그도 계약도 아닌, 게이트를 돌려 본 관찰)
>
> v0 → v1: kod-remastered 에서 모듈 2개(restored-project, engine)를 세우고 실제 변경 1건을
> 통과시키며 드러난 것만 반영했다. 추측으로 추가한 규칙은 없다.

---

## 템플릿

```markdown
---
module: engine                              # 트리 전체에서 유일한 slug
path: restored-project/Assets/Scripts/Engine # 리포 루트 기준. 게이트가 diff 매칭에 사용
schema: 1
status: draft                               # draft | active | frozen | deprecated
watch: .cs,.asmdef                          # 선택. R1/R12 가 "소스"로 볼 확장자
---

## 책임
<!-- 소유하는 것 1~3문장 + "하지 않는다" 한 문장 -->

## 진입점
<!-- 안내판. 공개 API 목록이 아니다. 보통 3개 안팎이지만 숫자가 기준은 아니다 -->
- `LeEngine.Run(scriptId)` — Runtime/LeEngine.cs

## 의존
### in (이 모듈이 쓰는 것)
- [[data]] — 변수 저장소 조회
### out (이 모듈을 쓰는 것)
- [[tools]] — 명령 테이블 미러 검증

## 불변식
- I1. `LeScriptContainer.SetLoader(null)` 은 `ArgumentNullException` 을 던진다 (근거: restored-project/Assets/Scripts/Engine/Runtime/LeScriptContainer.cs:171) [grep]
- I5. (폐기 2026-09-10 — [[engine]] I1 로 이관. 규칙은 살아 있고 소유자만 옮겼다)

## 미결
- 디렉토리 밖 133개 호출부를 `~With(conv)` 로 옮길지 미정
- 결정: 미설정 변수 확인 전용 문법을 언어에 추가한다 (예정)

## 이력
- 2026-09-10 I9 검증 수단 [grep]→[테스트] (run 0043)
```

---

## 필드별 규칙

| 필드 | 들어가는 것 | 안 들어가는 것 | 게이트 |
|---|---|---|---|
| 책임 | 소유 범위, 명시적 비소유 | 구현 설명 | — |
| 진입점 | 모듈을 **동작시키는 시작점** | 공개 API 전부, 데이터 타입, 값 객체, 내부 헬퍼 | — (사람이 큐레이션) |
| 의존 in/out | 다른 모듈 slug + 한 줄 이유 | 엔진·표준 라이브러리 | R3 대칭 |
| 불변식 | 참/거짓 판정 가능한 **한 문장** | 희망사항, 스타일 | R5 근거·태그, R6 소유, R8 중복, R11 근거 추적 |
| 미결 | 질문, 또는 결정됐으나 미구현 | 완료 항목, 작업 로그, 버그, TODO | R7 분할, R10 완료 표기 |
| 이력 | 계약 변경만 | 코드 변경 전부 | R2 |

**인용 경로는 어느 섹션이든 리포 루트 기준이다** — 진입점·의존·불변식 근거가 같은 기준을 쓴다. 게이트가 검사하는 것은 근거 줄뿐이므로(R12) 나머지 두 섹션은 사람이 지킨다.

**줄번호는 선택이다.** `파일.cs` 단독과 `파일.py::테스트명` 도 인용으로 세며, R6·R8·R11·R12 가 모두 본다.
줄번호 없는 인용은 파일 전체가 하나의 인용 지점이다 — R8 은 `파일.cs` 와 `파일.cs:10` 을 다른 지점으로 본다.
인용으로 세는 것은 소문자 확장자를 가진 토큰뿐이다. `LeAction.MakerDictionaryInit` 같은 멤버 표기는 파일이 아니다.

### 진입점 (v1 에서 정의 교체)

이 모듈을 처음 읽는 사람이 **어디서부터 읽어야 하는지** 알려주는 안내판이다. 공개 API 목록이 아니다.

- 디렉토리 밖에서 널리 참조되는 데이터 타입·값 객체는 진입점이 **아니다**. `의존` 이나 불변식으로 표현한다.
  (근거 사례: `LeValue` 는 디렉토리 밖 133개 파일이 참조하지만 진입점이 아니다. `LeTextInterpolator` 도 마찬가지)
- 판단 기준은 참조 수가 아니라 "이것을 부르면 모듈이 동작을 시작하는가".
- v0 표에 있던 "3개 초과 시 경고" 게이트는 **삭제**했다. 구현된 적 없고, 큐레이션은 기계 판정 대상이 아니다.

### 미결 (v1 에서 3상태로 명문화)

미결 항목은 다음 셋 중 하나다. 넷째는 없다.

1. **질문** — 아직 정하지 않은 것. 그대로 둔다
2. **결정됨, 미구현** — `- 결정: <무엇을 한다> (예정)`. 줄은 반드시 `결정:` 으로 시작하고 `(예정)` 으로 끝난다.
   날짜는 적지 않는다 — 그건 이력이 갖는 정보다. 구현되면 이 줄을 **삭제**하고 불변식을 신설한다
3. **완료** — 존재하지 않는다. 삭제된 상태다

"완료"라고 적힌 줄이 남아 있으면 승격 실패 신호다 (R10 이 운다).
승격 = 미결 줄 삭제 + 불변식 신설 + 이력 한 줄. 셋이 한 커밋에 있어야 한다.

**미결 → 불변식 승격 조건: 검증 수단이 존재할 것.** 근거 파일:라인을 적을 수 없고
[테스트|grep|리뷰] 중 하나로 판정할 수 없으면 아직 불변식이 아니다.

### 불변식 ID

- 한 번 붙인 ID 는 **재배열·재사용하지 않는다**. 번호에 빈칸이 생기는 것이 정상이다
- 폐기는 삭제가 아니라 묘비: `- I5. (폐기 <날짜> — 사유/이관처)`
- 묘비는 규칙이 아니므로 근거·검증 수단을 요구하지 않는다 (R5 면제)
- 근거의 파일 경로는 **리포 루트 기준**으로 적는다. 게이트가 소유 모듈을 찾는 기준이다

---

## 모듈 관계

- **부모-자식은 링크로 표현하지 않는다.** 디렉토리 포함 관계로 정의된다.
  루트 모듈과 하위 모듈 사이에 in/out 을 적지 않는다
- **순환 의존은 계약에 그대로 적는다.** 게이트는 순환을 금지하지 않고 R3 대칭만 본다 — 양쪽에 적으면 조용해진다.
  대칭을 맞추려고 한쪽을 비우면 계약서가 사실을 담지 못한다. 순환을 끊을지는 설계 판단이고 `미결` 에 남긴다
- **in/out 한 줄 이유는 누가 누구를 부르는지 주어가 드러나게 쓴다.** 양쪽 문장이 같은 방향을 가리켜야 한다.
  게이트는 대칭만 보므로 방향이 뒤집혀도 조용하다 — 사람이 읽는 방향이 틀린 계약서는 실패다
- 변경 파일은 **가장 깊은 모듈**이 소유한다
- 조상 모듈은 공용 테스트·설정의 보관처로 간주하여 R6 에서 면제된다
- 같은 규칙이 두 모듈에 있으면 **깊은 쪽이 소유**하고 얕은 쪽에 묘비를 남긴다
- **하위 모듈이 생기면 상위 `책임` 칸에서 그 범위를 빼고 `[[하위모듈]] 이 소유한다` 로 바꾼다.**
  와일드카드(`Foo.*`)로 범위를 적지 않는다 — 하위 모듈이 생길 때마다 거짓이 된다
- **규칙을 쪼개면서까지 소유권을 내리지 않는다.** 한 문장이 두 모듈의 심볼을 묶어야 성립하는 규칙(미러·대칭)은
  공통 조상이 온전히 갖는다. 반쪽만 내려보내면 판정 불가능한 문장 둘이 남는다
  (근거 사례: restored-project I9 는 `LeActionUtility`([[actions]])와 `LeActionKeys`(ScriptBinary)를 한 문장으로 묶는다)

### CLAUDE.md 어댑터

MODULE.md 가 있는 디렉토리에는 CLAUDE.md 가 있어야 한다 (R9). 어댑터는 재귀하지 않으므로
모듈마다 필요하다. 내용은 이 두 줄 + 프로젝트 고유 규칙:

```markdown
@MODULE.md

이 디렉토리의 계약은 MODULE.md 에 있다. 불변식을 깨는 변경은 먼저 MODULE.md 를 고치고 이력을 남긴다.
하위 디렉토리에 MODULE.md 가 있으면 그쪽이 더 깊은 소유자다. 그 파일을 함께 읽어라.
```

불변식 ID 를 CLAUDE.md 에 나열하지 않는다 — 낡는다.

---

## 게이트 규칙 (module-gate.mjs)

`status: active` 는 FAIL, 그 외는 WARN 으로 강등된다.
**예외: R6·R8·R12 와 R3 의 모듈 후보 경고는 언제나 WARN 이다** — `lvl()` 을 타지 않는다. 의도인지 사고인지는 아래 미결.

| 규칙 | 내용 |
|---|---|
| R0 | frontmatter `module`/`path` 존재, `path` 가 실제 위치와 일치 |
| R1 | 소유 소스 변경 ⇒ MODULE.md 변경 |
| R2 | 계약 섹션(책임·진입점·의존·불변식) 변경 ⇒ 이력 항목 추가 |
| R3 | 의존 in/out 대칭. 상대 MODULE.md 없으면 모듈 후보 경고 — 미결이 이미 모듈 후보로 적었으면 침묵 |
| R4 | 80줄 초과 ⇒ 분할 후보 |
| R5 | 불변식마다 근거 + [테스트\|grep\|리뷰] 태그 (묘비 면제) |
| R6 | 근거가 다른 모듈 소유이면 in/out 중 한쪽에 그 모듈이 있어야 함 (부모-자식은 양방향 면제) |
| R7 | "분할 후보: X" 가 가리키는 곳에 이미 MODULE.md 존재 ⇒ 정리 필요 |
| R8 | 같은 파일:라인을 두 모듈이 근거로 인용 ⇒ 중복 계약 |
| R9 | MODULE.md 있는데 CLAUDE.md 에 `@MODULE.md` 없음 |
| R10 | 미결에 완료 표기가 남아 있음 ⇒ 승격 실패 |
| R11 | 불변식이 근거로 인용한 파일이 변경됐는데 그 MODULE.md 미변경 (소유 무관) |
| R12 | 근거 경로가 리포 루트 기준이 아님(`--fix` 로 자동 정정), 또는 어디로도 해석되지 않음 |

R11 이 R1 의 사각지대를 덮는다: 검증 수단이 모듈 밖(공용 테스트 디렉토리)에 있을 때,
그 테스트가 약화돼도 R1 은 소유 모듈만 보므로 울지 않는다.

---

## 미결

- R6·R8·R12 와 R3 모듈 후보 경고가 `status: active` 에서도 WARN 인 것이 의도인지 미결. 승격의 효력이 근거 중복·근거 경로·모듈 밖 근거의 의존 누락에는 닿지 않는다 (`.harness/observations/02-gate-observation.md` [규칙 부족-3])
- R3 이 슬러그 대칭만 보고, 순환 쌍의 in/out 이 복제한 근거가 서로 일치하는지는 보지 않는다 — 한쪽만 고쳐도 조용하다 (engine in [[actions]] 와 actions out [[engine]] 이 같은 문장·같은 근거를 복제한다)
- 결정: `.harness/MODULE.md` 를 세울 때 `책임` 칸에 `observations/` 의 위치를 적는다 — 작업 로그(`.am/`, gitignored, 일회성)와 하네스 지식(`.harness/observations/`, 추적, 재생성 불가)을 어디서 가르는지가 그 문장에서 처음 명시된다. 관찰 기록은 게이트 규칙의 근거이자 "그 규칙이 왜 생겼나" 의 유일한 출처라 계약서와 같은 급이고, 분리될 때 하네스를 따라가야 한다 (예정)
- `watch` 가 확장자 목록만 받는다 — `tools/git-hooks/pre-commit` 처럼 확장자 없는 파일은 어느 모듈의 R1 에도 걸리지 않는다. 게이트를 부르는 훅 자신이 그 사각지대에 있다 (tools 미결이 구체 사례). 값 문법을 파일명까지 넓힐지 미결 — 필드 추가가 아니므로 "3개월간 지킬 것" 에 걸리지 않는다

---

## 3개월간 지킬 것

- 필드 추가 금지. 필요하면 `미결` 에 "스키마에 X 필드 필요?" 로 적고 기다린다
- MODULE.md 는 80줄 이내. 넘으면 모듈을 쪼갤 신호
- 엔진 특화 내용은 불변식 문장 안에만 등장하고 스키마에는 등장하지 않는다
- 구현되지 않은 게이트를 스키마 표에 적지 않는다 (v0 의 "3개 초과 경고" 사고)
