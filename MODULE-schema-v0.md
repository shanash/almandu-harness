# MODULE.md 스키마 v0

> 한 디렉토리(=모듈)가 지켜야 할 계약. 사람과 에이전트가 같이 읽고, 게이트가 diff로 검사한다.
> 원칙: **작업 로그가 아니라 계약서다.** "무엇을 했는지"는 `runs/`에, "무엇이어야 하는지"만 여기에.

---

## 템플릿

```markdown
---
module: battle              # 트리 전체에서 유일한 slug. 디렉토리명과 일치
path: Assets/Scripts/Battle # 리포 루트 기준. 게이트가 diff 매칭에 사용
schema: 0                   # 이 스키마 버전
status: active              # active | frozen | deprecated
---

## 책임
<!-- 이 모듈이 소유하는 것 1~3문장. "~하지 않는다"를 한 줄 넣으면 경계가 선명해짐 -->

## 진입점
<!-- 외부에서 이 모듈을 쓰는 첫 지점. 심볼명 + 파일. 3개 이내 -->
- `BattleController.StartBattle()` — Battle/BattleController.cs

## 의존
### in (이 모듈이 쓰는 것)
- [[inventory]] — 장비 스탯 조회만
### out (이 모듈을 쓰는 것)
- [[stage]], [[ui-battle]]

## 불변식
<!-- 깨지면 버그인 것. 검증 가능한 문장으로. 각 항목에 ID -->
- I1. 전투 중 `BattleState`는 BattleController만 변경한다
- I2. 턴 순서는 SPD 내림차순, 동점은 참가 순서
- I3. 이 모듈은 Addressables를 직접 로드하지 않는다

## 미결
<!-- 알지만 아직 결정 안 한 것. 결정되면 불변식이나 책임으로 승격하고 여기서 삭제 -->
- 회복 스킬의 오버힐 처리 방식

## 이력
<!-- 계약이 바뀐 것만. 최신이 위. run id로 상세 참조 -->
- 2026-09-09 I3 추가 (run 0042)
```

---

## 필드별 규칙

| 필드 | 들어가는 것 | 안 들어가는 것 | 게이트 |
|---|---|---|---|
| 책임 | 소유 범위, 명시적 비소유 | 구현 설명 | 비어 있으면 실패 |
| 진입점 | 외부 호출 지점 | 내부 헬퍼 | 3개 초과 시 경고 |
| 의존 in/out | 다른 모듈 slug + 한 줄 이유 | 엔진/표준 라이브러리 | out은 상대 모듈의 in과 대칭이어야 함 |
| 불변식 | 참/거짓 판정 가능한 문장 | 희망사항, 스타일 | 코드 diff 있는데 이 섹션 변경 없으면 리뷰어에게 "깨진 불변식 없음" 확인 요구 |
| 미결 | 결정 대기 항목 | 버그, TODO | 없음 |
| 이력 | 계약 변경만 | 코드 변경 전부 | 코드 diff 있으면 항목 1개 이상 추가 필수 |

---

## 왜 이 여섯 개인가

- **책임 + 진입점**: 에이전트가 "어디를 건드리면 되나"를 읽는 데 필요한 최소
- **의존**: 변경 영향 범위. 양방향으로 적는 이유는 게이트가 대칭 검사를 할 수 있어서
- **불변식**: 멀티 페르소나 리뷰의 질문지. ID를 붙이는 이유는 리뷰 결과를 "I2 위반"처럼 참조하기 위해
- **미결**: 불변식이 되기 전 대기실. 이게 없으면 사람들이 불변식에 모호한 문장을 넣음
- **이력**: 계약이 언제 왜 바뀌었는지. `runs/`로 가는 유일한 링크

## 3개월간 지킬 것

- 필드 추가 금지. 필요하면 `미결`에 "스키마에 X 필드 필요?"로 적고 기다린다
- 한 모듈의 MODULE.md는 80줄을 넘기지 않는다. 넘으면 모듈을 쪼갤 신호
- 엔진 특화 내용(Unity 컴포넌트, Unreal 클래스)은 불변식 문장 안에만 등장하고 스키마에는 등장하지 않는다

## 손으로 먼저 써볼 후보

kod-remastered에서 가장 잘 아는 모듈 1개, 가장 자주 깨지는 모듈 1개. 두 번째가 스키마를 더 많이 시험한다.