---
module: harness
path: .
schema: 1
status: active
watch: .mjs,MODULE-schema-v1.md
---

## 책임
MODULE.md 계약 체계 자체를 소유한다 — 스키마(`MODULE-schema-v1.md`), 커밋 때 계약을 판정하는 게이트(`module-gate.mjs`), 소비 리포에 그 체계를 놓는 설치 도구(`module-harness-init.mjs`)와 그 앞에서 패키지를 설치하는 스크립트(`almandu-harness-install.sh`, 패키지에 싣지 않는다), 셋의 회귀 테스트(`module-gate.test.mjs`·`module-harness-init.test.mjs`·`almandu-harness-install.test.mjs`), 게이트를 실제로 돌려 보고 남긴 관찰(`observations/`), 그리고 앞으로 무엇을 하기로 했는지(`DESIGN.md`, 5단계는 `DESIGN-review.md` 가 그 5절을 대체하고 세션 프롬프트는 `harness-stage5-prompts.md` 에 있다).
`observations/` 와 `DESIGN.md` 의 자리가 이 계약의 경계선이다 — 앞의 것은 규칙이 왜 생겼는지의, 뒤의 것은 다음 규칙이 왜 생길지의 유일한 출처라 둘 다 재생성되지 않는다. 그래서 계약서와 같은 급이고 이 디렉토리가 옮겨질 때 하네스를 따라간다. 일회성 작업 로그는 여기 두지 않는다.
프로젝트 고유 규칙은 담지 않는다 — 각 디렉토리의 MODULE.md·CLAUDE.md 는 그 디렉토리에 남고, 커밋을 실제로 막는 훅은 이 게이트를 설치한 리포에 있다. 이 패키지는 판정만 하고 차단은 종료 코드로 넘긴다.
변경을 계약 앞에 세우는 루프(`loop/`)는 [[loop]] 이 소유한다 — 배포 단위만 같고, 판정을 담지 않는 쪽이라 소유가 다르다.
리뷰 페르소나 프롬프트(`review/personas/`)와 커맨드 둘 다 여기 있다 — 판정도 판단도 담지 않는 텍스트라 게이트에도 루프에도 속하지 않는다. 커맨드는 자리가 둘이고 그 둘은 다른 것이다: `commands/` 는 소비 리포에 놓으려고 패키지에 싣는 일반형이고, `.claude/commands/` 는 이 리포가 자기를 위해 쓰는 사본이다. 페르소나 수는 질문 수를 따라간다 — 질문이 늘 때만 늘고, 10회 연속 통과한 질문은 내려서 준다 (DESIGN-review.md 3·6·11절).

## 진입점
- `npx --no-install almandu-module-gate [--staged|--base <ref>|--fix|--audit|--json]` — module-gate.mjs:657,664,669 (판정 결과를 종료 코드로 낸다)
- `npx --no-install almandu-module-gate --scope <경로>...` — module-gate.mjs:124 (판정하지 않는다: 그 경로를 고치려면 읽어야 할 계약을 깊은 것부터 낸다)
- `npm test` — module-gate.test.mjs:16 (게이트의 회귀 테스트 58개, 설치 도구 21개, 설치 스크립트 22개. 같은 명령이 [[loop]] 의 테스트도 함께 돌린다. 총 143개, ~71초)
- `npx --no-install almandu-module-gate --review` — module-gate.mjs:354 (판정하지 않는다: 이 diff 를 리뷰할 때 봐야 할 불변식을 태그별로 낸다)
- `npx --no-install almandu-harness-init [--dry-run] [--hooks-path <디렉토리>] [--no-config] [--override-hooks] [--no-commands]` — module-harness-init.mjs:66,80,97,108 (판정하지 않는다: 훅과 훅 배선·루트 문단·어댑터·커맨드를 놓는다 — 배선 하나는 작업 트리 밖(`git-common-dir/hooks/`)에 갈 수 있다)
- `bash almandu-harness-install.sh <대상 리포> [--ref <태그>] [--dry-run] [--override-hooks] [--no-commands] [--yes]` — almandu-harness-install.sh (판정하지 않는다: 대상 리포의 git 최상위에 package.json·.npmrc·.gitignore 를 맞추고 `npm --prefix` 로 설치한 뒤 설치 도구를 부른다. 그다음 훅이 쓰는 게이트 호출을 **그 문자열 그대로** 돌려 실제로 풀리는지까지 본다 — 풀리지 않으면 설치가 끝났어도 1 이다 (훅이 부르지 않는 것이 아니라 부르는 것이 실행되지 않는 것이라 3 이 아니다). 패키지에 싣지 않아 클론이나 태그의 raw URL 에서 돈다. 종료 코드 0 설치·훅 연결, 1 실패·거부, 2 사용법, 3 설치했지만 남의 것을 말없이 바꾸지 않고는 배선할 수 없다 — 0.9.0 부터 init 이 가리지 않고 배선할 수 있는 자리에는 스스로 배선하므로 3 은 그 일곱 경우로 좁아졌다, README 종료 코드 표)
- 옛 bin 이름 `module-gate`·`module-harness-init`·`module-loop` — 0.7.0 부터 별칭이다. 같은 파일을 가리키고, 소비 리포에 이미 놓인 훅이 옛 이름을 부른다 (package.json bin). 소비 리포가 이 bin 을 부르는 형태(절대화 경로 / 상대 경로 / 이름 해석에 기대는 실행기를 안 쓴다)는 README 설치 절의 경로 형태 표가 정한다 — npx 는 워크스페이스 멤버에서 풀리지 않는다 (2026-09-22)
- `MODULE-schema-v1.md` — 사람과 에이전트가 계약을 쓸 때 읽는 규칙서
- `review/personas/*.md` — 리뷰 패킷 하나에 답 하나를 내는 질문. 각 파일은 질문·입력 필드·출력 형식·답하지 말 것 네 절이다. 파일은 셋이고 활성은 하나다 — `contract-checker.md` 는 2026-09-16, `scope-watcher.md` 는 2026-09-17 에 내려졌고 파일은 기록으로 남겼다 (DESIGN-review 3절, 활성 목록은 [[loop]] 의 `ALL_PERSONAS`). 0.6.0 부터 패키지에 실린다 — 소비 리포에서는 `node_modules/almandu-harness/review/personas/` 다.
- `commands/*.md` — 소비 리포의 `.claude/commands/` 에 놓이는 커맨드. 설치 도구가 디렉토리째 복사하고 이름 목록을 자기 안에 두지 않는다 (I7). 셋이다 — `/module-work`(계약→수정→게이트→리뷰→커밋), `/module-review`(패킷과 판정자), `/module-draft`(계약서 초안). 0.8.0 부터 패키지에 실린다 — 소비 리포에서는 `node_modules/almandu-harness/commands/` 다. 이 리포 자신의 `.claude/commands/` 는 소스 경로를 쓰는 별개 사본이다. 커맨드가 루프를 부르는 형태도 절대화 경로다(README 표)

## 의존
### in (이 모듈이 쓰는 것)
- 없음. node 표준 라이브러리 세 모듈과 `git` CLI 만 쓴다 — 설치할 의존이 없어 어느 클론에서나 그대로 돈다 (module-gate.mjs:12-14). 설치 스크립트는 bash·npm·네트워크를 쓰지만 게이트를 놓기 전에만 돌고 패키지에 실리지 않는다 — 게이트의 의존(I4)이 아니다
### out (이 모듈을 쓰는 것)
- 없음 — 이 패키지를 쓰는 리포가 자기 `in` 에 `(외부: almandu-harness)` 로 적는다. 소비자를 여기 세면 리포가 늘 때마다 거짓이 된다 (R14)

## 불변식
- I1. 판정 수준은 `status` 로만 갈린다 — `active` 는 FAIL, 그 외는 WARN 으로 강등된다. 예외는 R3 의 모듈 후보 경고 하나로 언제나 WARN 이다 (근거: module-gate.mjs:285-286,462-466, module-gate.test.mjs:126,617) [테스트]
- I2. 차단 수단은 종료 코드뿐이다 — FAIL 이 하나라도 있으면 1, 없으면 0 이고 WARN 은 0 이다. `--json` 은 같은 판정을 다른 표면으로 낼 뿐이라 봉투의 `fail` 이 종료 코드와 동치다 (근거: module-gate.mjs:657,664,669) [테스트]
- I3. 회귀 테스트는 이 리포의 계약서를 입력으로 쓰지 않는다 — 임시 git 저장소에 fixture 를 세워 돌리므로 계약서가 바뀌어도 테스트는 그대로다 (근거: module-gate.test.mjs:5-7,16) [테스트]
- I4. 게이트는 외부 의존 없이 돈다 — import 는 node 표준 세 줄이고 나머지는 `git` 서브프로세스다 (근거: module-gate.mjs:12-14, 재현: module-gate.mjs 에서 `from 'node:` 3건) [grep]
- I6. 어댑터 문구의 출처는 스키마 하나다 — 설치 도구는 `MODULE-schema-v1.md` 의 어댑터 블록을 읽어서 쓰고, 자기 안에 사본을 두지 않는다. 사본이 생기는 순간 "로드되는 자리마다 같은 문장" 을 아무도 검증할 수 없다 (근거: module-harness-init.mjs:28-34, module-harness-init.test.mjs:60) [테스트]
- I5. 게이트는 자기 판단을 파일에 쓰지 않는다 — 유일한 쓰기는 `--fix` 의 근거 경로 정정이고 그것도 `근거:` 가 있는 줄만 건드린다 (근거: module-gate.mjs:626-630,639) [리뷰]
- I7. 소비 리포에 놓이는 커맨드 문구의 출처는 패키지의 `commands/` 하나다 — 설치 도구는 그 디렉토리를 읽어 그대로 복사하고 파일 이름 목록도 문구도 자기 안에 두지 않으며, 설치 스크립트는 커맨드를 놓지 않는다. 사본이 생기는 순간 어느 판이 원본인지 아무도 대조할 수 없다 (근거: module-harness-init.mjs:108-120, module-harness-init.test.mjs:131) [테스트]

## 미결
- R13 의 비용은 건수가 아니라 범위 폭이다 (2026-09-12 실측: 모듈 디렉토리 범위 25회 0.19s, `restored-project/Assets` 트리 범위 25회 3.2s). 예산을 64 로 올렸지만 통제는 "범위는 경로 하나" 규칙이 맡는다 — 트리 범위 주장이 여럿 생기면 그 규칙을 좁힐지 미결이고, 지금 그런 주장은 gameplay I2 하나다
- `watch` 에 `.md` 는 `MODULE-schema-v1.md` 하나만 들어 있다 — README.md 와 `observations/` 는 R1 밖이다. 의도한 것이지만(관찰은 규칙이 아니라 증거다) 스키마가 바뀌는 커밋만 이 계약서를 함께 요구한다는 뜻이라 관찰 기록의 유실은 아무도 잡지 않는다
- 게이트가 자기 계약을 판정한다 — 이 계약서를 어기는 게이트 변경을 그 게이트가 잡을 수 있는지는 순환이고, fixture 테스트가 그 순환을 끊는 유일한 수단이다. 자기 판정의 사각지대 목록은 없다
- 훅이 게이트를 파일 경로로 부르므로 **끌어올려진 설치**에서는 선다 — 손으로 `npm i -D -w <멤버>` 를 하면 패키지가 워크스페이스 루트로 가고 `<멤버>/node_modules/almandu-harness/module-gate.mjs` 가 없다. 설치 스크립트는 그 배치를 만들 수 없고(`--prefix` + `--workspaces=false`, 아니면 phase 5 가 죽는다) 실패는 닫히는 쪽이라(없는 절대 경로를 이름 대고 exit 1) 막지 않고 적어만 둔다. **답 (2026-09-23)**: 해소 경로는 늘리지 않았다 — 훅 본문은 어느 자리에 놓이든 `GATE` 상수 한 줄이고 조건 분기가 없다. 늘어나는 것은 **배선 자리**이고, phase 7 은 그것을 텍스트 검사(`gate_hook`)와 해소 검사(`sh -c "$GATE_CALL --scope ."`)로 나눠서 본다 — 자리가 늘어도 검사는 하나다. 유일한 미검증 층("전역 훅이 정말 그 파일을 체인하는가")은 텍스트 휴리스틱으로 남았고, install test 21 이 실제 `git commit` 으로 그 층을 검증까지 올린다
- 같은 휴리스틱(git 이 pre-commit 으로 실제 실행할 파일이 무엇인가)이 `module-harness-init.mjs` 의 `hookState()` 와 `almandu-harness-install.sh` 의 `check_wired`/`HOOK_STATE` 에 두 벌 산다 — 언어가 갈라 공유할 파일이 없다. 같은 입력에서 같은 답이 나오는 것으로 통제하고, 갈라지면 행위자(init)와 다른 답을 낸 쪽(스크립트)이 실물 파일을 다시 재는 phase 7 에서 3 으로 드러나지 조용한 거짓 초록으로 새지 않는다 — 단 그 완전성은 `check_wired` 가 **다른 파일**을 보는 경우까지는 못 미치고, install test 21 이 실제 커밋으로 그 틈을 막는다
- 연결된 워크트리에서는 배선 자리가 메인 리포의 것이다 (`git rev-parse --git-common-dir` 이 링크된 워크트리에서 메인 `.git` 을 가리킨다). `module-harness-init.mjs` 의 `isLinkedWorktree()` 는 그때 아무것도 안 쓰고 스크립트는 검사가 없다 — 이 PC 에 연결된 워크트리가 없어(설계 8절 위험 7 실측) 양성 대조를 할 대상이 없다. 픽스처를 세울 대상이 생기면 닫는다

## 이력
- 2026-09-23 0.10.0 — 설치 스크립트가 이미 설치된 git 태그를 다른 태그로 올릴 수 있게 한다. kod-remastered 를 `#v0.7.0` → `#v0.9.0` 으로 올리다 phase 4 가 npm 12 에서 EALLOWGIT 로 죽었다 — 하네스는 직접 의존이고 `.npmrc` 는 `allow-git=root` 인데도. **원인은 npm 소스에서 확인했다**: reify 가 extract 에 넘기는 `_isRoot` 는 "프로젝트 루트의 **유효한** 간선을 만족하는가" 이고(`reify.js:747`), 루트 간선은 락 루트 레코드(`packages[""].devDependencies`)의 옛 spec 에서 오므로 새 ref 의 노드가 root 가 아니게 된다. 로컬 `git+file://` 베어 리포로 오프라인 재현해 후보 일곱을 쟀고(design.md 1절 표), 락 루트 레코드 **한 키만** 새 spec 으로 바꾼 t7 이 원인을 따로 떼어 냈다. 고침은 그 한 키를 `act` 를 지나 맞추는 것이다 — spec 이 바뀌고 그 키가 락에 있을 때만, 먼저 `package-lock.json.almandu.$$` 로 떠 두고, npm 이 성공하기 전에 끝나면(실패·INT·TERM) EXIT trap 이 바이트째 되돌린다 — 되돌리지 못하면 사본을 지우지 않고 그 자리를 알린다. 복원을 trap 한 곳에 둔 것은 `/am:verify` 의 지적이다: 첫 판은 실패 분기에서만 되돌려 중단되면 trap 이 사본만 지웠고, 복원 실패 메시지가 가리킨 사본도 곧바로 trap 이 지웠다. `trap 'exit 143' TERM` 은 장식이 아니다 — 이 머신의 bash 3.2 는 TERM 으로 죽을 때 EXIT trap 을 돌지 않아, 그 줄을 빼면 아래 E 가 운다. 재검증이 하나를 더 잡았다: bash 는 npm 이 도는 동안 받은 신호를 npm 이 끝난 **뒤에** 처리하므로, 플래그만 보면 성공한 설치의 락을 옛 spec 으로 되돌린다 — 그래서 되돌리기 전에 `package.json` 이 아직 옛 spec 인지, 즉 npm 이 아직 저장하지 않았는지를 본다 — 되돌린 락은 늘 `package.json` 과 같은 쪽에 선다. 첫 판은 락의 `resolved` 를 비교했는데 세 번째 검증이 두 태그가 한 커밋을 가리키면 그 비교가 눈을 감는다고 지적했고, 실제로 그 판에서 G 가 운다. 고르지 않은 셋: uninstall 뒤 install(kod 에서 실제로 쓴 우회로다 — 둘 사이에 의존이 package.json 에서 사라져, 설치가 실패하면 훅이 닫히고 `npm i` 로 못 돌린다), 락 삭제(소비 리포의 다른 의존 락까지 다시 만든다), 그 한 명령에만 `--allow-git=all`(소비 리포의 다른 패키지가 끌어오는 git 의존까지 풀린다). npm 판본으로 가르지 않았다 — npm 11 이하에서 무해하고, 한 경로여야 CI(npm 10)가 같은 코드를 돈다. **테스트 하나를 더했다** (설치 스크립트 22) — tarball 에서 version 만 다른 태그 둘을 세운 git 리포로 올리기(A)·예행연습(B)·없는 태그 실패 시 락 복원(C)·`dependencies` 에 둔 의존(D)·npm 도중 TERM 을 받은 뒤 락 복원(E)·npm 이 성공한 뒤 처리된 TERM 은 되돌리지 않음(F)·같은 커밋 별칭 태그에서도 락과 package.json 이 같은 spec(G) 일곱을 잰다. **양성 대조는 두 번 했다**: `act` 줄을 지우면 B 가 먼저 울어 A 를 가렸으므로, `act` 는 두고 쓰기만 no-op 로 바꿔 A 가 단독으로 EALLOWGIT 로 우는 것을 확인했다. 복원 쪽은 셋 — 복원을 끄면 C 가, TERM trap 을 빼면 E 가, 저장 확인을 빼면 F 가, 그것을 `resolved` 비교로 바꾸면 G 가 운다. A 는 npm 12 에서만 결함을 드러낸다 — CI(npm 10)에서는 고침 전에도 초록이다. 버전은 사용자 결정으로 minor 다(DESIGN 7절 — 표로는 patch 자리). 1.0 은 조건이 찼지만 미뤘다(같은 절). 게이트·스키마·설치 도구는 열지 않았으므로 I1~I7 은 그대로다. `DEFAULT_REF="v0.10.0"` 을 같은 커밋에서 — 태그를 밀기 전까지 `--ref` 기본 경로는 죽는다. 테스트 142→143, `npm test` 실측 ~71초. shellcheck 0.11.0 0건(0.10.0 은 이 머신에 없다)
- 2026-09-23 [C] 설치 정책 서술을 [B] 의 코드에 맞춘다 — `README.md`(훅 정책 문단을 "가리지 않는다" 와 "git 디렉토리 밖에는 얹지 않는다" 로 쪼개고, 종료 코드 3 의 일곱 경우, init 이 놓는 것 다섯, 재클론 복구 두 줄), `GUIDE.md`(0.9.0 기준, `core.hooksPath` 가 비어 있는 것이 정상인 리포와 그때 볼 "배선" 행 — 확인표 여섯 줄 → 일곱), `DESIGN.md`(7절에 정책을 쪼갠 기록과 고르지 않은 셋, 10절 첫 미결 닫기, GUIDE 적재를 미루는 근거를 "0.9.0 이 된다" 에서 "한 판본을 살아 본 뒤" 로, CI 블록의 낡은 테스트 수 132 → 142). 이 계약서에서는 설치 스크립트 진입점의 종료 코드 3 문구 하나를 옮겼다 — [B] 가 뜻을 좁혔는데 그 자리는 옛 뜻을 들고 있었다. 코드·테스트는 한 줄도 안 고쳤으므로 I1~I7 은 그대로이고 테스트는 142 그대로다. `watch` 에 이 문서들이 없어 R1 은 요구하지 않는다 — 진입점 문구가 거짓이 되어 적는다
- 2026-09-23 [B] 를 놓는다 — 설치 도구가 훅 충돌을 스스로 판단한다 (harness-multi-repo-deploy design.md 6-B). `module-harness-init.mjs`: `:36-39` 를 GATE 상수 하나(`node "$(git rev-parse --show-toplevel)/node_modules/almandu-harness/module-gate.mjs"`) + HOOK + CHAIN 으로 제자리 재구성 — 만드는 훅의 바이트는 전과 같다(`cmp` 로 대조, 아래). `:67`·`:74` 를 `mayConfig()` 판단으로 바꿨다(제자리 두 줄). 파일 끝에 5절(`hookState`·`mayConfig`·`wire`)을 놓고, `wire()` 호출을 커맨드 블록 뒤·출력 루프 앞에 둔다. `wire()` 는 git 이 pre-commit 으로 실제 실행할 파일을 3-A 의 규칙으로 찾아 — 리포 안·비추적·실행 가능·sh/bash shebang 일 때만 없으면 만들고 있으면 shebang 뒤에 CHAIN 을 얹는다. husky v9 의 `.husky/_` 디스패처는 정규화해 `.husky/pre-commit` 에 배선한다. `--override-hooks` 가 새 플래그다. `almandu-harness-install.sh`: `check_wired`/`predict_wired` 를 "SET_CONFIG=1 이면 `$ROOT/$HOOK_DIR/pre-commit`, 아니면 3-A 의 실행될 파일" 하나의 축으로 재구성 — 옛 `*)` arm 이 SET_CONFIG=0 인 fresh/default-hooks 에서도 `$HOOK_DIR/pre-commit` 만 보던 버그(진짜 실행 자리인 `DEF_HOOK_DIR` 를 안 봄)를 고쳤다. `call_init` 이 `--override-hooks` 를 전달한다. `:616` 의 자작 config 계획 줄을 지웠다 — init 의 `--dry-run` 이 이제 `DRY + git config core.hooksPath .githooks` 를 스스로 낸다(계획의 저자가 하나가 됐다). phase 8 `*)` arm 은 지우지 않고 문구만 일반화했다(여섯째 경우의 유일한 안내라는 성질은 그대로다). `:47` 주석 좌표를 `:36`(GATE)·`:38-39`(HOOK·CHAIN)로 고쳤다. **cmp 대조**: `git show HEAD:module-harness-init.mjs` 로 옛 판을 뜨고 두 임시 리포에서 각각 `--no-config --no-commands` 로 낸 `.githooks/pre-commit` 을 `cmp` 로 댔다 — 동일. **테스트**: `module-harness-init.test.mjs` — `newRepo`/`init` 을 `GIT_CONFIG_GLOBAL`·`GIT_CONFIG_NOSYSTEM=1`·`git init --template=` 로 격리(4단계, 이 머신의 전역 hooksPath 가 없으면 `mayConfig()` 판단이 갈렸다 — 격리 없이 먼저 돌려 실제로 test 1 이 이 머신에서 빨간 것을 확인했다). 새 `test()` 여섯 — ① 전역 체인(만든다, 설정은 안 켠다) ② 남의 `.git/hooks/pre-commit`(shebang 뒤 삽입, 멱등, 원본 보존) ③ 안 쓰는 경우 여섯(추적 훅·비체인 전역 훅·local-outside — 리포 밖에 아무것도 안 생기는지까지 — 에 더해 **추적 디렉토리에 `pre-commit` 만 없는 경우**, **실행 권한 없는 남의 훅**, **sh 계열이 아닌 shebang**) ④ `--override-hooks` ⑤ `--dry-run` 두 arm(fresh 는 config 계획을 내고 `.git/hooks` 배선은 계획하지 않는다, 남의 훅은 바이트 불변) ⑥ husky v9(디스패처 바이트 불변, `.husky/pre-commit` 에 배선). `almandu-harness-install.test.mjs` — **재정의 셋**: test 12(`.git/hooks` 에 다른 훅이 있어도 `commit-msg` 는 그대로 두고 `pre-commit` 을 채운다 → 0), test 13(b)(`unchained` — 체인 자리에 훅이 없으면 만든다 → 0, `--override-hooks` 없이), test 16(첫 arm — `--no-config` 라도 진짜 실행 자리(`.git/hooks`)가 비어 있으면 채운다 → 0, 여섯째 arm 은 3 그대로 — `mayConfig()` 가 참이라 `wire()` 가 2단계에서 빠진다). test 13(a) 에 예행연습 arm(이미 게이트를 부르는 훅이면 `predict_wired()` 가 실행 전에 0 을 미리 본다). test 20 에 체인 블록 arm(얹힌 줄이 `<call> --staged || exit $?` 이고 `GATE_CALL`·`_line` 과 같다). test 21 을 뒤집고 얹기 arm 을 더했다 — (a) 훅이 없던 체인 자리는 exec 판을 새로 만들어 위반 커밋을 막는다, (b) 남의 `.git/hooks/pre-commit` 이 있던 체인 자리는 shebang 뒤에 얹어 원래 줄(`exit 0`)은 파일에 남기고 위반 커밋을 막으며 전역 훅은 계속 돈다. 두 arm 모두 커밋 출력에 `R0` 가 있는지까지 본다 — 막은 것이 게이트의 판정이어야 한다. (b) 는 먼저 `--dry-run` 이 0 을 예측하는지 본다. **설계와 다르게 간 자리 셋** — (1) 설계 6-B 7단계 ③ 은 arm 을 "셋" 으로 닫았지만 리뷰 3라운드의 남은 MEDIUM(거부 분기 둘에 테스트가 없다)을 닫으려고 실행 권한·shebang arm 을 더했다. (2) 첫 구현은 추적 검사를 **파일**에만 걸어, 추적 디렉토리에 `pre-commit` 만 없는 리포(kod 의 `tools/git-hooks` 모양)에서 새 훅을 만들었다 — 5-B 1-a 가 금지한 바로 그것이다. 추적 검사를 **그 디렉토리에 추적 파일이 있는가**까지 넓혔다(`isTrackedPlace()`, 스크립트의 예측도 같은 선). husky ⑥ 은 `.husky/` 에 추적 파일이 없는 픽스처라 그대로 선다. (3) 첫 구현의 `wire()` 는 설정을 켤 리포에서도 돌아 fresh 의 `--dry-run` 이 config 계획과 `.git/hooks` 배선을 **둘 다** 냈다 — 5-B 2단계대로 `!noConfig && mayConfig()` 면 먼저 빠진다. 같은 이유로 `predict_wired()` 는 설계 5-C 가 적은 "git 디렉토리 안의 실행 가능 훅이면 1" 을 빠뜨리고 있어 얹기 경우를 3 으로 예측했다 — 채웠다. **양성 대조 다섯을 돌리고 되돌렸다**: `-x` 검사를 지우면 ③, shebang 검사를 `#!` 로 풀면 ③, 디렉토리 추적 검사를 끄면 ③, `wire()` 의 조기 반환을 지우면 ⑤, `predict_wired()` 의 얹기 분기를 지우면 test 21 이 운다(각각 그 하나만). **셋 다 `test()` 수는 그대로다(21) — 전부 arm 이다.** `package.json` 0.9.0, `DEFAULT_REF="v0.9.0"` 을 같은 커밋에서 — 태그를 밀기 전까지 `--ref` 기본 경로는 죽지만(4-D, 회귀 테스트는 `--spec`/스텁으로 안 밟는다) 실물 설치는 v0.9.0 태그가 필요하다. **I1~I5 는 그대로다**(`module-gate.mjs` 를 안 열었다). I6·I7 의 인용은 자리를 지켰다(`:28-34`·`:66,80,97,108`·`:108-120` 전부 0 밀림, `shiftAbove()` 가 제자리 수정을 순증감 0 으로 본다) — 단 `module-harness-init.test.mjs` 는 파일 끝 규약이 없어 헬퍼 격리 두 줄이 위에 들어갔고, I6·I7 이 인용하는 그쪽 좌표(`:50`→`:60`, `:121`→`:131`)를 같은 커밋에서 옮겼다. **미결 둘을 새로 올렸다** — 같은 휴리스틱이 `.sh`·`.mjs` 두 벌 사는 것, 연결된 워크트리에서 배선 자리가 메인 리포인 것(둘 다 이 PC 에 픽스처를 세울 실물이 없다). **미결 `:47`(끌어올려진 설치)에 답했다** — 해소 경로는 하나로 남았고 늘어난 것은 배선 자리이며, 유일한 미검증 층(전역 훅이 실제로 체인하는지)은 install test 21 이 실제 커밋으로 검증까지 올렸다. **테스트**: 설치 도구 15→21, 총 136→142, `npm test` 실측 ~71초(142 전부 통과). `time npm test` 로 쟀고 README:124 를 옮겼다. shellcheck 는 이 머신에 0.11.0 하나만 있어 그것만 돌렸고 0건이다(얹기 예측의 shebang `case` 패턴이 SC2026 을 내 따옴표를 고쳤다)(0.10.0 은 못 구했다 — 2026-09-21 이력의 교훈대로 로컬 초록이 CI 초록을 보장하지 않는다는 것을 알고 있다). `node module-gate.mjs --base HEAD~1` 자기 판정은 FAIL 0. 실물 `--dry-run` 은 다른 리포를 건드리므로 이번엔 돌리지 않았다(6-D 배포 실행은 이 리포의 커밋이 아니다)
- 2026-09-23 [A] 를 놓는다 — 훅 배선을 코드로 넓히기 전에(harness-multi-repo-deploy design.md 6-A), 실측 세 자리를 회귀 테스트로 먼저 고정한다. `almandu-harness-install.test.mjs` 만 고쳤고 `module-harness-init.mjs`·`almandu-harness-install.sh` 는 열지 않았다. **test 21(양성 대조, 체인 배선)** — 전역 훅이 리포의 `.git/hooks/pre-commit` 을 체인해도, 오늘의 install 은 그 자리에 아무것도 얹지 않는다: 설치는 3, R0 를 어기는 `MODULE.md` 를 스테이지한 **실제 `git commit`** 은 통과한다(게이트가 안 돈다). 전역 훅이 실제로 돌았는지는 전역 훅의 echo 를 커밋 출력에서 직접 재고(공회전이 아님을 보증), 훅이 아직 없는 상태가 조용히 지나가는 것도 같은 arm 이 잰다(`.git/hooks/pre-commit` 부재 단언). 주석에 "[B] 가 이 단언을 뒤집는다" 를 적었다 — [B] 이후 이 커밋은 막혀야 한다. **test 20 파싱 경화** — `exec ` 줄을 `find` 한 결과를 `assert.ok` 로 먼저 받는다. 지금은 없으면 `.slice` 가 예외를 던져 무엇이 틀렸는지 말하지 않았다. **test 16 여섯째 경우 arm** — 설정은 켤 수 있는데(fresh) `.githooks/pre-commit` 이 이미 있고 게이트를 안 부르는 경우(3-C). 별도 리포(`f.mk('sixth')`, `--no-config` 없이)에서 exit 3·`localHooksPath==='.githooks'`(우리가 켰다 — test 8 은 이 값을 못 잰다)·바이트 불변·출력이 파일 이름과 덧붙일 줄을 함께 내는지 넷을 잰다. **양성 대조를 돌리고 되돌렸다** — `almandu-harness-install.sh` phase 8 `*)` arm(`:692-694`, 여섯째 경우의 유일한 안내문)의 두 `say` 줄을 지우고 `npm test` 를 돌려 test 8·test 16 **둘**이 빨간 것을 실측했다(설계 3-C 의 예측과 일치). test 16 은 첫 arm(`foreign`, `--no-config`)이 같은 `*)` 문구에 먼저 걸려 예외를 던지므로 그 안의 여섯째 arm 이 실제로 자기 몫을 재는지는 별도로 격리해 확인했다 — 여섯째 arm 만 먼저 도는 임시 순서로 돌리면 `assert.match(...module-gate.mjs" --staged \|\| exit \$\?/)` 에서 단독으로 빨갛고, 출력엔 "게이트가 연결되지 않았다" 뒤에 안내가 없었다. 그 임시 순서와 `.sh` 편집은 파일을 원래대로 되돌린 뒤 확인했다(`git diff --quiet -- almandu-harness-install.sh` 통과). **I1~I7 은 그대로다** — 게이트·스키마·설치 도구·`package.json` 은 한 줄도 고치지 않았다. R1(`watch: .mjs`)이 이 커밋에 이 계약서를 요구한다. 테스트는 135→136(설치 스크립트 20→21, `test()` 수만 늘었다 — 여섯째 경우는 arm 이라 안 늘린다)
- 2026-09-22 앞 커밋이 "별 건" 으로 남긴 스키마의 npx 둘을 닫고, 같은 부류가 다시 생기면 우는 테스트를 놓는다 — `MODULE-schema-v1.md:101,192` 는 남은 npx 중 **계약을 쓰는 사람에게 읽으라고 시키는 유일한 처방문**이었다 (`:24` 진입점). `module-gate.mjs:3-10`·`module-harness-init.mjs:3` 도 `files` 에 실리지만 같은 급이 아니다: 저쪽은 아무도 읽으라고 안 한 파일 머리의 사용법 주석이고 I1/I2/I4/I5·I6 의 근거 인용이 딸려 와 옮기면 밀린다. 규칙서는 워크스페이스 멤버 소비자가 그대로 따라 치면 거기서 매달린다. **상대 경로가 아니라 절대화를 골랐다** — README 표의 상대 행은 "그 자리에 '리포 루트에서 부른다' 가 적혀 있다" 를 조건으로 다는데 규칙서 본문에는 그 문장이 없고, 넣는 것보다 cwd 를 묻지 않는 형태를 쓰는 편이 한 줄 덜 믿는다. 그래서 README 표의 절대화 행에 `MODULE-schema-v1.md` 본문을 **자리로 적었다** — 규칙의 출처가 그 표 하나라는 앞 줄의 주장은 표가 실제 자리를 다 셀 때만 참이다. **테스트를 하나 놓았다** (설치 도구 15) — `README.md`·`GUIDE.md`·`MODULE-schema-v1.md`·`commands/*` 의 어느 줄도 npx 로 bin 을 부르라고 시키지 않는지 본다. bin 목록은 `package.json` 에서 읽어 별칭이 늘어도 따라온다. 이력·설계는 밖이다(거기 npx 는 처방이 아니라 당시의 문자열이다) — `loop/MODULE.md:55` 가 그 경우다. **이 가드가 안 보는 것 셋을 적어 둔다**: 축이 `npx` 문자열 하나라 `npm exec`·`pnpm dlx` 는 지나가고, 감시 목록이 하드코딩이라 새 문서는 조용히 빠지며, `loop/loop.mjs` 의 die 메시지가 내는 리포 상대 경로(소비 리포에서 거짓 안내다)는 npx 가 아니라 원리적으로 밖이다. 지금 그 셋의 실제 위반은 앞의 둘이 0건이고 셋째가 11줄이다. **양성 대조를 먼저 돌렸다**: 스키마를 고치기 전에 이 테스트가 `MODULE-schema-v1.md:101` 에서 빨간 것을 확인했다. 앞 줄이 걱정한 대로 총계가 134→135 로 늘어 이 계약서의 `:19` 와 README 의 숫자를 같은 커밋에서 옮겼다. **세 자리를 더 고쳤다** — (a) 이미 설치된 리포의 이행 절차가 `files` 에 없는 `GUIDE.md:244` 에만 있어 실리는 README 에도 적었다. `put()` 이 있는 파일을 덮지 않으므로 `npm i` 만으로는 옛 커맨드가 그대로 남는다 — 2026-09-21 줄이 훅에 대해 (a) 로 적은 것과 같은 귀결인데 커맨드 쪽은 아무 데도 없었다. (b) `almandu-harness-install.test.mjs:211` 의 주석이 "상대 경로판은 여기서 죽고 절대화판만 산다" 로 적혀 있었는데, `loopCall()` 의 정규식이 닫는 따옴표를 요구해 상대 경로판은 cwd 질문에 닿기 전 `assert.ok(m)` 에서 먼저 죽는다. 결과는 같은 빨강이지만 그 arm 이 증명하는 것은 cwd 하나뿐이라 문구를 거기까지 좁혔다 — 주석이 넓으면 다음 사람이 이 arm 을 "cwd 무관성 증명" 으로 믿는다. (c) README 의 `~74초` 를 실측(71.5~71.7초)에 맞춰 `~72초` 로 내렸다. **I1~I7 은 그대로다** — 스키마를 고쳤지만 어댑터 블록은 한 글자도 안 건드렸고(I6), 편집 둘이 제자리라 `:101`·`:192` 도 그 자리이며 블록 좌표도 안 밀렸다. 게이트·설치 도구·루프·`package.json` 은 한 줄도 고치지 않았다. `watch` 에 `MODULE-schema-v1.md` 가 있어 R1 이 이 커밋에 이 계약서를 요구한다 — 앞 두 줄과 달리 적는 것이 선택이 아니다
- 2026-09-22 소비 리포가 부르는 형태를 문서 전체에서 파일 경로로 옮긴다 — `commands/module-work.md`(5자리)·`commands/module-review.md`(2자리)·`README.md`·`GUIDE.md`(21건)·`loop/MODULE.md:24` 가 여전히 `npx --no-install` 을 시키고 있었다. 원인은 훅에서 고친 것과 같다(2026-09-21 줄): 워크스페이스 멤버에서 npm 이 멤버의 `.bin` 을 안 본다. 형태를 **둘로 나누고 경계를 적었다** — cwd 를 보장할 수 없는 자리(훅·루트 문단·커맨드·설치 스크립트 phase 7)는 `$(git rev-parse --show-toplevel)` 로 절대화하고, "리포 루트에서 부른다" 가 그 자리에 적혀 있는 자리(README·GUIDE 실행 블록·CI)는 상대 경로다. 규칙의 출처는 README 설치 절의 표 하나이고 결정은 DESIGN 7절의 "템플릿의 경계" 표다 — 그 표는 `DESIGN.md:369` 와 `module-harness-init.test.mjs:189` 가 이미 인용하고 있었는데 **없었다**. 인용을 지우지 않고 대상을 만들었다. **증명을 붙였다** — test 5(hwatu 회귀, 워크스페이스 멤버 + 127 로 죽는 npx 스텁)에 arm 하나를 더해, 놓인 `/module-work` 에서 꺼낸 문자열을 **리포 루트가 아닌 하위 디렉토리**에서 돌려 종료 코드 0 과 npx 미경유를 함께 고정했다. 문자열을 테스트에 다시 적지 않고 놓인 커맨드에서 꺼내는데(`loopCall()`), 다시 적으면 phase 7 이 `GATE_CALL` 을 공유하기 전에 갈라져 있던 자리가 테스트 쪽에 새로 생긴다. `almandu-harness-install.test.mjs:475` 의 `/almandu-module-loop/` 단언은 같이 죽었다 — 어느 설계 문서도 그 자리를 안 적어 뒀고, 바로 아래 페르소나 쌍과 같은 꼴(문구가 가리키는 파일이 실제로 실려 왔는지까지)로 바꿨다. `SUBS` 표(`module-harness-init.test.mjs:191`)는 같은 커밋에서 옮겼다 — 따로 가면 test 14 가 중간 커밋에서 운다. `GUIDE.md` 를 처음 추적한다: 미추적인 동안 R1 도 R11 도 `--audit` 도 안 봐서 0.7.0 시점 문장 여섯이 남아 있었고 그중 `:18`(훅 내용 기대값)은 **제대로 설치된 리포가 통과할 수 없는 확인**이었다. `files` 에는 넣지 않았다 — 넣으면 minor 라 0.9.0 이 되고 `DEFAULT_REF` 가 없는 태그를 가리켜 기본 `--ref` 와 `--dry-run` 이 죽는다(앞 줄 둘과 같은 판단). `MODULE.md:17,18,20,21` 의 npx 는 **고치지 않았다**: 그 넷은 부르는 법이 아니라 패키지가 낸 bin 을 세는 자리이고 bin 은 하나도 안 죽었다. `module-gate.mjs:3-10`·`module-harness-init.mjs:3`·`MODULE-schema-v1.md:101,192` 도 남겼다 — 앞의 둘은 I1/I2/I4/I5·I6 의 근거 인용이 딸려 오고, 스키마는 `files` 에 실리는 규칙서라 별 건이다. `almandu-harness-install.test.mjs:377` 은 `MODULE.md` 2026-09-21 줄이 봉인한 그대로다. 게이트·스키마·설치 도구·루프·`package.json` 은 한 줄도 고치지 않았으므로 I1~I7 은 그대로다. 앞 줄이 적은 `:555`·`:594` 는 이번 커밋에서 +19 밀렸다(`:574`·`:613`) — 앞 줄은 고치지 않는다(당시의 좌표가 기록이다). 테스트는 늘지 않았다 (134 그대로) — 워크스페이스 arm 을 새 `test()` 로 만들지 않고 test 5 안에 넣었다. 새 `test()` 면 20→21 이 되어 이 계약서의 `:19` 와 README 의 134 를 같은 커밋에서 옮겨야 하는데, test 5 가 이미 워크스페이스 부모·멤버·npx 스텁을 다 갖고 있어 빌릴 것이 없었다
- 2026-09-22 앞 커밋이 남긴 미인용 리다이렉트 둘을 닫는다 — `almandu-harness-install.test.mjs:555`·`:594` 의 스텁이 `> ${seen}`·`> ${npmSeen}` 로 경로를 보간한다. 앞 줄이 이 둘을 **일부러 남긴** 이유("뒤따르는 단언이 `readFileSync` 라 공백 경로에서 시끄럽게 실패하므로 거짓 초록이 아니다")는 그대로 참이고, 이번에 닫는 이유는 다르다: 시끄러운 실패도 공백 든 `TMPDIR` 에서 test 19 를 통째로 못 돌게 만든다. 양성 대조는 앞 두 번과 형태가 다르다 — 거짓 초록이 아니므로 "고침 전 단언이 통과한다" 가 안 서고, `TMPDIR="/private/tmp/a b" npm test` 로 **고침 전 test 19 가 `readFileSync` ENOENT 로 빨갛고 고침 뒤 초록**임을 확인했다. 판정도 표면도 규칙도 바뀌지 않았으므로 I1~I7 은 그대로이고, 제자리 두 줄이라 인용 밀림은 0 이다. 테스트는 늘지 않았다 (134 그대로)
- 2026-09-21 shellcheck 코드가 판본마다 갈린다 — `SC2329` 와 `SC2317` 을 둘 다 끈다. 0.8.0 이력이 "로컬(0.11.0)에서 먼저 돌려 11건을 닫았다 … 일곱은 SC2329 오탐이다" 라고 적은 것은 한 판본만 본 것이다: 같은 `act()` 간접 호출을 0.11.0 은 함수 머리에 SC2329 로, 0.10.0 이하는 본문 줄마다 SC2317 로 낸다 (2026-09-21 실측: 0.10.0 에서 35건 exit 1, 0.11.0 에서 0건 exit 0). 그 문장은 **고쳐 쓰지 않는다** — 무엇을 덜 봤는지가 기록이고 정정이 사는 자리는 여기다 (D5 정정과 같은 처리). **그 스텝은 CI 에서 한 번도 돈 적이 없었다**: `67e62cd` 에는 스텝이 없었고(0.8.0 이 스크립트와 함께 넣었다), 그것을 담은 첫 푸시 `a63465b` 는 `npm test` 에서 죽어 `skipped` 였다. 오늘 훅 수정으로 test 5 가 초록이 되자 그제서야 처음 실행됐고 거기서 빨갛다 — "로컬 초록이 CI 초록을 보장하지 않는다" 는 0.8.0 의 경고가 맞았고, 그것을 **볼 수 있게 되기까지 커밋 둘이 걸렸다** 는 것이 적을 값이다. 실패를 가리는 것은 다른 실패다. 고친 것은 파일 머리 지시자 한 줄과 그 위 주석이다 — 옛 주석의 `(0.11.0 기준 7건)` 은 숫자가 아니라 "판본마다 갈린다" 를 빠뜨린 것이 문제였고, 숫자만 고치면 다음 사람이 같은 함정에 빠진다. **양성 대조를 먼저 돌렸다** — CI 급 판본(0.10.0)을 받아 `1aee5d9`·`a63465b`·`44adb11` 셋 모두 35건 exit 1 인 것을 확인했다. 이번 커밋이 만든 것이 아니라 0.8.0 부터 있던 것이고, 줄번호만 앞 커밋의 +8 만큼 밀렸다. 게이트·스키마·설치 도구·커맨드·테스트는 고치지 않았으므로 I1~I7 은 그대로다. `watch` 에 `.sh` 가 없어 R1 은 이 변경에 계약서를 요구하지 않는다 — 그래도 적는 이유는 앞 줄의 기록이 불완전해졌기 때문이다. 테스트는 늘지 않았다 (134)
- 2026-09-21 테스트 스텁의 리다이렉트 대상을 인용한다 — `almandu-harness-install.test.mjs:417` 의 npm 스텁이 `>> ${npmLog}` 로 경로를 따옴표 없이 보간한다. `npmLog` 는 `mkdtempSync(join(tmpdir(), …))` 에서 오므로 `TMPDIR` 에 공백이 있으면 셸이 리다이렉트 대상을 첫 조각까지만 읽고 뒤 조각을 `printf` 인자로 먹어, `:426` 의 `existsSync(npmLog) === false` 단언이 **npm install 이 실제로 불려도 통과**한다 — 음성 단언이 조용히 공회전하는 자리다. 앞 커밋이 같은 부류를 `:194` 에서 하나 닫았고(그쪽은 훅이 npx 를 지났는지 묻는 단언이었다) 이것이 마지막 거짓 초록이다. **실측으로 확인하고 고쳤다** — 공백 든 디렉토리에서 npm install 을 실제로 부르게 해 고침 전 `existsSync` 가 `false`(단언 통과, 거짓 초록), 고침 후 `true`(단언이 운다). 양성 대조 없이는 이 줄이 무엇을 고쳤는지 아무도 보여 줄 수 없다 — 09-21 정지 가드 줄이 적은 것과 같은 이유다. 같은 미인용 리다이렉트인 `:555`·`:594` 는 **고치지 않았다**: 뒤따르는 단언이 `readFileSync` 와 `existsSync(...) === true` 라 공백 경로에서 시끄럽게 실패하므로 거짓 초록이 아니고, 함께 고치면 이 커밋이 실측으로 확인한 결함 하나를 넘어선다. 판정도 표면도 규칙도 바뀌지 않았으므로 I1~I7 은 그대로이고, 제자리 한 줄이라 인용 밀림은 0 이다. 테스트는 늘지 않았다(134 그대로) — 있던 단언 하나가 비로소 자기 일을 한다
- 2026-09-21 훅이 게이트를 파일 경로로 부른다 — `exec node "$(git rev-parse --show-toplevel)/node_modules/almandu-harness/module-gate.mjs" --staged`. `npx --no-install almandu-module-gate` 는 대상 리포가 npm 워크스페이스의 **멤버**일 때 풀리지 않는다: 설치 스크립트는 EALLOWGIT 때문에 일부러 멤버에 설치를 고정하는데(`--prefix` + `--workspaces=false`) npx 는 `--prefix` 없이 돌아, npm 이 조상의 `workspaces` 를 보고 localPrefix 를 워크스페이스 루트로 옮기고 npm 11 이하는 멤버의 `node_modules/.bin` 을 보지 않는다. 실측 2026-09-21: **11.17.0 실패, 12.0.2 통과**. 하한(npm 10)은 이 머신에 그 판본이 없어 **재현하지 못했고**, 근거는 CI 35563610187(node 20 ⇒ npm 10)이 test 5 에서 빨간 것 하나다 — 실측과 같은 급으로 적지 않는다. **D5 정정**: `.am/harness-deploy-script/design.md:107` 이 "이 배치에서 맨 npx 훅이 선다" 고 적은 것은 npm 12 한 대에서 돌린 실험을 일반화한 것이고, 그것이 오늘 CI 가 빨간 직접 원인이다. 그 문장은 고쳐 쓰지 않는다 — 무엇을 잘못 일반화했는지가 기록이고, 정정이 사는 자리는 여기다. 부수로 닫힌 것 하나: `--no-install` 은 **실행**을 막을 뿐 조회를 막지 않아, 이름이 안 풀리면 레지스트리 요청이 나가고 거기서 매달린다(양쪽 npm 에서 실측). 사람 없이 매 커밋마다 도는 훅에 그 자리를 남겨 둘 수 없는 것이 npx 를 아예 뺀 둘째 이유이고, README 의 선점 방지 문장도 그만큼 좁혔다. 0.7.0 이력의 같은 문장은 **고치지 않는다**(당시의 판단이 기록이다). 구조 쪽으로는 스크립트가 그 문자열을 한 번만 들고(`GATE_CALL`) phase 7 이 그것을 `sh -c` 로, phase 8 이 붙여 넣을 줄로 쓴다 — 사본이 셋에서 둘로 줄었고 둘의 동일성을 테스트 20 이 고정한다. 버그가 exit 3 에 안 잡힌 이유가 정확히 거기다: `gate_hook()` 은 "훅이 게이트를 부르는가"(텍스트)를 묻고 phase 7 은 "그 호출이 풀리는가"(해소)를 묻는데, phase 7 이 명령을 공유하지 않고 다시 적어 둔 탓에 둘이 갈라져도 아무도 몰랐다 — I6·I7 을 만든 이유를 한 번도 적용하지 않았던 문자열에 적용했다. phase 7 실패는 그대로 1 이다: 훅은 옳은 줄을 들고 있으니 3 의 안내("이 줄을 덧붙여라")가 거짓이 된다. **닫히지 않은 것 둘을 분명히 적는다** — (a) 이미 설치된 소비 리포는 그대로다. `put()` 이 있는 파일을 덮지 않으므로 그쪽 훅은 훅 파일을 지울 때까지 npx 형태로 남고, 그래서 옛 bin 별칭 셋을 계속 둔다. 테스트 13 의 픽스처 훅이 옛 문구인 것은 실수가 아니라 그 호환을 지키는 증거라 **고치지 않는다**. (b) **연결된 워크트리는 여전히 깨져 있다** — `git worktree` 는 훅의 cwd 와 `--show-toplevel` 을 링크된 워크트리 루트로 주는데 거기엔 보통 `node_modules/` 가 없다. 옛 형태도 새 형태도 거기서 죽으므로 이번 변경이 그것을 닫았다고 읽으면 안 된다. 끌어올려진 설치도 미결로 올렸다. 게이트·스키마·커맨드·`package.json` 은 한 줄도 고치지 않았으므로 I1~I7 은 그대로이고, `module-harness-init.mjs` 의 세 자리와 테스트의 두 자리는 전부 **줄 수가 같은 제자리 수정**이라 인용 밀림이 0 이다(`:28-34`·`:66`·`:80`·`:97`·`:108-120`, 테스트의 `:50`·`:121`). 버전은 올리지 않았다 — `files` 도 `bin` 도 안 늘었고 이미 통과하던 계약서가 FAIL 이 되지도 않으며, `v0.8.0` 태그를 아직 밀지 않았으므로 앞 줄과 같은 판단이다(올리면 `DEFAULT_REF` 가 없는 태그를 가리켜 기본 `--ref` 와 `--dry-run` 이 `ls-remote` 에서 죽는다). 테스트 하나를 더했다 (설치 스크립트 20). **양성 대조를 돌리고 넣었다** — 이 머신은 npm 12 라 test 5 가 고치기 전에도 초록이어서, 훅 본문만 npx 로 되돌려 넷이 우는 것을 확인했다 (test 5 의 npx 스텁 arm 127, test 20 사본 0개, init 1·8)
- 2026-09-21 설치 스크립트에 정지 가드를 건다 — `http.lowSpeedLimit=1`·`http.lowSpeedTime=60` 을 `GIT_CONFIG_*` 환경변수로 내보내, 60초 동안 사실상 한 바이트도 움직이지 않는 연결을 git 이 끊게 한다. 막는 것은 **이미 붙은 연결이 멈추는 경우** 하나다 — 상대가 연결을 받아 놓고 아무것도 보내지 않는 자리가 유일하게 경계가 없었고, README 가 `curl … | bash` 비대화형 실행을 지원한다고 적은 이상 CI 에는 Ctrl-C 가 없다. **연결이 맺어지지 않는 경우는 가드 밖이다**: lowSpeed 는 전송이 시작된 뒤에만 재고 git 은 connect 타임아웃을 노출하지 않아 OS 기본값에 맡겨진다(리눅스에서 2분을 넘기기도 한다). 첫 판에는 그 자리를 "OS 가 이미 끝낸다" 고 적었는데 재검증이 과장이라고 지적했고 그 지적이 맞다 — 끝나는 것과 빨리 끝나는 것은 다르다. `git -c` 가 아니라 환경변수인 이유는 우리 `ls-remote` 말고 **npm 이 git 의존을 받으려고 부르는 git 서브프로세스**에도 닿아야 하기 때문이다 — npm 의 `--fetch-timeout` 은 레지스트리 HTTP 만 보고 그 git 은 보지 않아, `--ref` 경로가 실제로 매달리는 자리는 npm 쪽이다. 그 상속은 자식이 env 를 물려받는다는 일반론으로 두지 않고 테스트 19(C)가 고정한다: npm 이 부른 git 만 스텁으로 가로채 받은 값을 적게 했고, `npm_config_offline=true` 에서도 npm 이 git 스펙에는 git 을 부른다는 것도 함께 확인했다. `timeout` 은 쓰지 않았다: 이 파일이 자기 머리에 적은 이식성 약속(bash 3.2·Git Bash)에 GNU coreutils 가 없고 macOS 기본에도 없다. 값이 1 바이트/초인 것은 대역폭 제한이 아니라 정지 감지라 느린 회선을 자르지 않기 위해서다. 부르는 쪽이 이미 `GIT_CONFIG_COUNT` 로 자기 설정을 주입하고 있으면 덮지 않고 가드를 통째로 건너뛰는데, 그것을 조용히 하지 않고 `warn` 한 줄을 낸다 — 이 파일이 상태 이탈을 늘 한 줄로 알리는 관례이고, 말하지 않으면 매달리는 이유를 아무도 못 찾는다. 그 줄은 사용법 뒤에 둔다: 앞에 두면 `--help` 가 경고부터 뱉는다. git 2.31 미만이 이 변수를 무시하는 것은 그대로 대가로 남는다. 종료 코드 규약은 그대로다 — 멈춘 연결은 이미 있던 `die` 를 타고 1 로 나간다. 버전은 올리지 않았다: `v0.8.0` 태그를 아직 밀지 않았으므로 이 변경은 그 태그가 담을 내용의 일부다. 게이트·스키마·설치 도구·커맨드는 고치지 않았으므로 I1~I7 은 그대로다. 테스트 하나를 더했다 (설치 스크립트 19, 경우 셋). 그 테스트는 **가드를 빼면 우는 것을 확인하고** 넣었다 — 정지 가드는 없어도 평소에는 아무 증상이 없어서, 양성 대조 없이는 통과만 하는 테스트가 된다
- 2026-09-20 0.8.0 설치 스크립트와 커맨드 — `almandu-harness-install.sh` 를 리포 루트에 놓고, 소비 리포용 커맨드 셋(`commands/`)을 패키지에 싣는다 (DESIGN.md 7절 설치). 스크립트가 생긴 이유: hwatu-cli 에서 npm 이 부모 디렉토리의 package.json 을 프로젝트 루트로 잡아 대상 리포의 `.npmrc` 를 읽지 않았고(EALLOWGIT), 그 실패는 패키지가 설치되기 전에 일어나 init 이 막을 수 없다 — 그래서 bin 이 아니라 패키지 밖의 스크립트다. git 최상위에 package.json 을 만들고 `npm --prefix` 로 걷기를 끊은 뒤 설치 자리와 부모가 그대로인지 확인한다. 훅은 이미 있는 core.hooksPath(로컬·전역)도, `.git/hooks` 에서 돌고 있는 훅도 기본으로 바꾸거나 가리지 않는다 — init 이 로컬 값을 덮어쓰고, hooksPath 를 켜면 git 이 `.git/hooks` 를 통째로 무시하고, 이미 있는 pre-commit 을 건너뛰므로, 게이트가 연결되지 않으면 종료 코드 3 과 덧붙일 한 줄을 낸다. 훅 충돌을 init 이 판단하게 만드는 것은 여전히 다음 변경이다 (DESIGN.md 10절). 커맨드를 실은 이유: 설치가 끝난 리포에서 곧바로 `/module-work` 로 작업을 시작할 수 있어야 한다는 요청이다. 0.6.0 은 "커맨드는 경로가 리포마다 달라 소비 리포가 사본을 둔다" 며 싣지 않았는데, 그 전제가 틀렸다 — 리포마다 다른 것은 소스를 체크아웃해 둔 이 리포뿐이고 소비 리포의 경로는 전부 `node_modules/almandu-harness/…` 로 같다. 놓는 것은 스크립트가 아니라 init 이다: 놓는 주체가 둘이 되면 안 되고(이 리포가 훅 본문에 대해 이미 내린 판단), init 이 놓으면 `npm i` 만 하는 업그레이드에서도 새 커맨드가 들어온다. 그 문구의 출처를 하나로 못 박은 것이 새 불변식 I7 이고, 이미 있는 파일은 덮어쓰지 않으므로 소비 리포가 고쳐 둔 커맨드는 안전하다. init 의 새 코드는 3단계 **뒤에** 붙였다 — 위쪽에 줄을 더하면 이 계약서가 인용한 `:28-34`·`:66`·`:80`·`:97` 이 전부 밀린다. 위쪽에서 고친 것은 사용법 주석(`:3`·`:5-6`)뿐이고 줄 수가 같은 제자리 수정이라 밀림이 0 이다: R11(b) 가 보는 것은 수정이 아니라 밀림이다. **"설치하면 바로 작업" 은 대상에 커밋이 하나는 있을 때다** — `/module-work` 의 1단계가 HEAD 를 읽는데 루프에도 게이트에도 가드가 없다. 설치와 첫 커밋은 커밋 0 개에서도 서므로 거부하지 않고, 스크립트가 감지해 첫 커밋을 다음 블록의 0 번으로 낸다 (진짜 고침은 DESIGN.md 10절). 이 리포의 `.claude/commands/module-review.md` 와 실리는 `commands/module-review.md` 는 경로 둘 말고는 같아야 하고, 한쪽만 고치면 새 테스트가 운다 — 루프는 내려진 페르소나만 막고 새로 는 페르소나가 배포판에서 빠지는 것은 조용하기 때문이다. 실리는 판에는 "원본은 …" 머리 줄을 두지 않았다: 그 줄이 있으면 두 사본이 경로 치환만으로 같아질 수 없어 그 테스트가 설 수 없다. `files` 에 `commands/` 가 늘어 minor 이고(7절 버전 표), 그래서 기본 ref 도 v0.8.0 이라 태그를 밀기 전까지 `--ref` 기본 경로는 서지 않는다 — `--dry-run` 도 함께 선다. 태그 확인은 예행연습도 거치기 때문이다: 그 확인을 예행연습만 건너뛰면 쓰기 전에 죽는 실패를 예언하지 못해 예행연습이 거짓 초록불을 내고, README 와 설계가 적은 "`git ls-remote` 만 네트워크를 본다" 도 거짓이 된다. 게이트·스키마는 고치지 않았으므로 I1~I5 는 그대로이고, I6 이 인용한 `adapterText()` 도 그대로다. 테스트 스물넷을 더했다 (설치 도구 6, 설치 스크립트 18)
- 2026-09-18 0.7.0 — 패키지 이름을 `module-harness` 에서 `almandu-harness` 로 바꾼다. bin 은 새 이름(`almandu-module-gate`·`almandu-harness-init`·`almandu-module-loop`)을 내고 옛 이름 셋을 같은 파일을 가리키는 별칭으로 남겼다 — 소비 리포에 이미 놓인 훅이 `npx --no-install module-gate` 를 부르고, 설치 도구는 있는 훅을 덮어쓰지 않는다. 파일 이름·게이트 출력 접두사(`module-gate:`)·상태 디렉토리(`.git/module-loop/`)는 그대로다: 파일 이름을 바꾸면 근거 인용이 전부 옮겨 가는데 이력은 옛 이름을 들고 있고, 상태 디렉토리를 옮기면 소비 리포의 `keep-verdict.mjs` 가 결과 파일을 못 찾아 조용히 통과한다. 설치 도구가 쓰는 훅과 루트 문단은 새 이름을 부른다. 패키지 이름은 별칭을 둘 수 없어 소비 리포의 `node_modules/module-harness/…` 경로와 R14 표지 `(외부: module-harness)` 는 그쪽에서 설치 목록과 한 커밋으로 옮겨야 한다 — 따로 움직이면 R14 가 운다 (README 이름 변경 절). 0.x 동안 호환성 파괴는 minor 로 쓴다 (DESIGN.md 7절). 스키마는 R14 예시와 bin 이름만 바꿨고 어댑터 블록은 건드리지 않았다(I6). 게이트는 한 줄도 고치지 않았으므로 I1~I6 은 그대로다. 옛 이력·observations 의 `module-harness` 는 고치지 않는다 — 당시의 이름이 기록이다. 소비 리포에 안내하는 `npx` 호출에는 `--no-install` 을 붙였다 — 새 이름은 npm 에 아직 아무도 올리지 않아, 로컬 설치가 없는 머신에서 맨 `npx` 는 레지스트리로 넘어가 남이 선점한 패키지를 받아 실행할 수 있다. 테스트 하나를 더했다 (설치 8)
- 2026-09-18 0.6.0 — 루프(`loop/loop.mjs`, bin `module-loop`)와 페르소나(`review/`)를 `files` 에 싣는다. 소비 리포(kod-remastered)가 `file:` 심링크를 떠나 태그로 설치하면 심링크로 닿던 루프·페르소나가 사라지기 때문이다 (DESIGN.md 2절 운영). 한 태그가 게이트·루프·페르소나를 함께 고정하는 쪽을 골랐고, 대가는 루프 CLI 가 공개 표면이 된 것이다 — 그 결정과 버전 규칙은 DESIGN.md 7·10절에 있다. minor 인 이유는 표면이 늘었을 뿐 이미 통과하던 계약서를 FAIL 시키지 않기 때문이다. `loop/` 의 테스트·계약서·어댑터와 리뷰 커맨드는 싣지 않았다 — 어댑터가 실리면 소비 리포에서 루프를 Read 할 때 이 리포의 계약이 섞이고, 커맨드는 경로가 리포마다 달라 소비 리포가 사본을 둔다. 게이트·스키마·설치 도구는 고치지 않았으므로 I1~I6 은 그대로다
- 2026-09-17 0.5.0 — 5e 동안 얼려 둔 루프 결함 셋(observations/05 결함 메모 6·1·7)을 고친 버전을 찍는다. 고친 곳은 전부 [[loop]] 이고 무엇을 왜 그렇게 골랐는지는 그 이력에 있다: 커밋 시점에 패킷을 다시 만들어 리뷰한 트리와 대조하고(`diff.content`), 통과가 아닌 답을 `Review-Verdict:` 트레일러로 남기고, `scope` 가 트리에 없는 경로를 거부한다. minor 인 이유는 패킷 필드·트레일러 줄·거부가 하나씩 늘었을 뿐 이미 통과하던 계약서를 FAIL 시키지 않기 때문이다 (DESIGN.md 7절). 게이트·스키마·설치 도구는 고치지 않았으므로 I1~I6 은 그대로다. 패킷 스키마와 트레일러 형식은 DESIGN-review 2·4·5절에 함께 적었고 13절 미결 한 줄이 닫혔다
- 2026-09-17 5e 종료 — 범위 감시자를 리뷰에서 내리고 5단계를 닫는다. 종료 조건 ②·③ 은 찼고 ①(실전 검출)은 19커밋에서 0 이다. DESIGN-review 10절은 "셋 다" 와 "셋 다 없음" 만 정해 가운데가 비어 있었고, 이 경우를 **종료**로 읽는 결정을 그 절 끝에 적었다 — ✗ 를 충족으로 고쳐 읽지는 않았다. 기다리지 않은 근거는 `observations/05` 결론 절에 있다: 남은 질문으로는 ① 에 닿을 길이 사실상 없고, 루프를 얼려 둔 탓에 결함 6 이 trailer 네 개를 틀리게 남겼다. 범위 감시자는 14회 연속 `예` 에 더해 질문이 이미 loop I2 였다 — 그 `아니오` 는 커밋될 수 없는 묶음에서만 나온다 (DESIGN-review 3절). 불변식 판정자 하나를 남겼고, 대상이 없으면 돌지 않아 켜 두는 비용이 없다. 5단계 결론은 DESIGN.md 2절 상태표에도 적었다. 게이트는 고치지 않았으므로 I1~I6 은 그대로다
- 2026-09-16 5e — 계약 대조자를 리뷰에서 내린다. DESIGN-review 6절의 역방향 경로("10회 연속 예/참이면 내린다")가 처음 돌았고, 그것이 곧 10절 종료 조건 2 의 충족이라 조건 셋 중 둘(2·3)이 찼다 — 남은 것은 조건 1(실전 검출)뿐이다. 내림의 근거와 **그 근거가 약한 이유**(10회 중 계약 diff 가 실질인 것은 4회, 아예 없던 것이 4회라 나머지는 페르소나 규정상 자동 통과)를 DESIGN-review 3절에 함께 적었다 — 되돌릴 조건도 거기 있다. 게이트는 고치지 않았으므로 I1~I6 은 그대로다. 활성 목록을 든 `loop/loop.mjs` 와 그것을 부르는 `.claude/commands/module-review.md` 가 함께 바뀌었고, 페르소나 파일 셋은 하나도 지우지 않았다
- 2026-09-13 5e-3 — 05 의 결함 메모 첫 항목과 DESIGN-review 13절 미결 한 줄을 적는다: 비통과 verdict 가 커밋 시점에 소실된다. 고치지 않았다 — 5e 중에 루프를 고치면 관찰 중인 도구가 바뀌어 앞뒤 회차를 같은 셈에 넣을 수 없다. 이것이 앞 줄(5e-1)의 전제 A 가 확인 불가로 끝난 구조적 원인이고, 승격 경로의 유일한 원료가 그 이력이라 미결로 올렸다. 결함을 적는 자리는 관찰이고 결정을 적는 자리는 설계라 둘 다 한 줄씩이다
- 2026-09-13 5e-2 — `observations/05` 를 **모든 수치 0 으로** 다시 연다. 앞 판(5e-0)은 모집단을 trailer 로 못 박고도 그 trailer 9건을 그대로 세어 계약 대조자 9·범위 감시자 9 로 시작했는데, 그 9건이 전부 하네스 자기 커밋과 5a~5d 회차다 — 계약 밀도가 실제 프로젝트와 달라 같은 분모에 들어가면 오탐률이 낙관적으로 희석되고, 10회 판단도 실전을 한 건도 보지 않은 채 도달한다. 그래서 05 에 **셈의 규칙** 절을 새로 놓아 무엇이 분모에 들어가는지를 문서 안에 닫았다: kod-remastered 의 실제 작업 커밋만, 연속은 누계가 아니라 시간순. 종료 조건(DESIGN-review 10절)은 고치지 않았고 9절 상태 기록만 갱신했다. 게이트도 페르소나도 고치지 않았으므로 I1~I6 은 그대로다
- 2026-09-13 5e-1 — `observations/04` 에 정정 절을 덧붙인다. 원본 표·서술은 한 글자도 고치지 않았다: 무엇을 잘못 셌는지가 기록이고 고치면 그 기록이 사라진다. 정정 둘 — 04 의 "실전 검출 1건(5b)" 은 trailer 9건이 전부 통과라 재현되지 않으므로 종료 조건 1 을 **미충족**으로 되돌렸고, "범위 감시자 연속 7회" 는 누계이며 시간순 마지막 비통과가 P3 재실행이라 연속은 0 이다. 원인은 하나다 — 되돌려 커밋이 없는 프로브와 trailer 가 있는 실전을 같은 셈에 넣었다. 앞 줄(5e-0)이 승계를 거부한 판단은 그대로이고 이번 것은 그 근거를 04 쪽에도 남긴 것이다. 게이트도 페르소나도 고치지 않았으므로 I1~I6 은 그대로다
- 2026-09-13 5e-0 — 실전 관찰 문서 `observations/05-review-field.md` 를 개설하고 이미 관찰된 것을 이관한다. 카운터 모집단을 커밋 `Review:` trailer 하나로 못 박았다 (DESIGN-review.md 8절 결정) — 04 가 적은 "연속 통과 7회" 는 되돌린 프로브와 정상 커밋을 섞은 누계이고 P3 의 `아니오` 가 가운데 있어 연속도 아니라서, 승계하면 10회 판단이 근거 없이 시작된다. 04 의 "실전 검출 1건(5b)" 도 승계하지 않았다: 지목된 커밋의 trailer 는 전부 통과이고 verdict 전문이 어디에도 없다. 원본 관찰인 04 는 고치지 않았고, 왜 그 검출이 남지 않았는지(루프가 커밋과 함께 패킷·결과를 버린다)를 05 의 결함 메모에 적었다. 게이트도 페르소나도 고치지 않았으므로 I1~I6 은 그대로다
- 2026-09-13 0.4.1 — 5d 재실행에서 04 의 페르소나 결함 둘이 사라졌다(7회 판정에 펜스 0, 계약 대조자의 근거는 두 번 다 파일의 실제 줄). 검출·통과 패턴은 1차와 같다 — 프롬프트 수정이 판정을 바꾸지 않고 표기만 고쳤다는 뜻이다. patch 인 이유는 규칙도 표면도 늘지 않고 프롬프트 문구만 바뀌었기 때문이다. 두 번째 수정 회차는 하지 않았다 — 프롬프트를 계속 만지면 어느 버전이 관찰된 것인지 흐려진다
- 2026-09-13 5d 결함2 — 계약 대조자의 근거 줄번호가 어느 좌표인지 못 박는다. 04 의 관찰: 낸 근거 `MODULE.md:30`·`:22` 는 파일의 실제 줄이 아니라 `contract_diff` 안에서 센 줄이었다. 패킷의 diff 는 계약 칸만 잘라 붙인 것이라 좌표가 원본과 다르고, 그대로 남으면 나중에 아무도 그 줄을 못 찾는다. 이 페르소나만 고쳤다 — 불변식 판정자는 원본 파일을 직접 읽으므로 같은 혼동이 없고, 범위 감시자는 줄번호를 적지 않는다
- 2026-09-13 5d 결함1 — 페르소나 셋의 출력 형식 절에 "코드펜스로 감싸지 않는다" 를 명시한다. 04 의 관찰: 11회 판정 중 4회가 ```json 펜스로 감싸 왔다. 답이 틀린 것이 아니라 표기가 갈린 것이고, 사람이 옮기면 문제가 없지만 경로 A 를 자동화하면 파싱이 갈린다. "답하지 말 것" 이 아니라 출력 형식 절을 고쳤다 — 펜스는 질문 밖의 말이 아니라 같은 답의 다른 표기다
- 2026-09-13 5c — kod-remastered 에서 프로브 3종과 정상 커밋 3건을 돌린 관찰을 `observations/04-review-observation.md` 에 남긴다. 검출 3/3, 오탐 0/3. 관찰이 프로젝트가 아니라 여기 사는 이유는 이 디렉토리가 이미 적은 대로다 — 규칙이 왜 생겼는지의 출처는 하나여야 한다. 게이트도 루프도 고치지 않았다
- 2026-09-13 0.4.0 — 5a·5b 가 들어간 버전을 찍는다. 루프의 새 표면 둘(`review --packet`·`--answer`)은 규칙을 바꾸지 않고 이미 통과하던 계약서를 FAIL 시키지도 않으므로 minor 다 (7절 버전 표). `files` 화이트리스트는 그대로라 패키지 표면은 늘지 않았다 — `loop/` 와 `review/` 는 아직 이 리포 안에서만 쓰인다. 도구가 만드는 사본(`.agents/`·`AGENTS.md`)은 무시 목록에 넣었다: 커맨드와 어댑터의 원본은 `.claude/` 와 `CLAUDE.md` 하나씩이고, 사본이 커밋되면 어느 쪽이 원본인지 아무도 못 센다 (I6 과 같은 이유)
- 2026-09-12 5b — 리뷰 페르소나 셋(`review/personas/`)과 그것을 서브에이전트로 돌리는 `/module-review` 커맨드 초안을 놓는다 (DESIGN-review.md 3·5절). 게이트에도 루프에도 넣지 않았다: 판정도 판단도 담지 않는 텍스트이고, 루프에 넣으면 "답을 만들지 않는다" 는 그쪽 경계가 흐려진다. 각 파일의 네 번째 절(**답하지 말 것**)이 가장 길다 — 페르소나가 서로의 질문을 답하기 시작하면 어느 질문이 무엇을 잡았는지 셀 수 없고 승격 경로가 무너진다. 게이트의 규칙도 표면도 바뀌지 않았으므로 I1~I6 은 그대로다
- 2026-09-12 5단계 설계를 문서로 세운다 — `DESIGN-review.md`(멀티 페르소나 리뷰)와 그것을 세션 단위로 돌리는 `harness-stage5-prompts.md`. DESIGN.md 5절을 고치지 않고 별도 파일로 둔 것은 그 절이 4b 까지의 결정을 담고 있어 덮어쓰면 왜 그렇게 갔는지가 사라지기 때문이다. `watch` 에는 넣지 않았다 — 설계 문서를 코드 커밋마다 요구하는 것은 계획이 아니라 세금이라는 앞 줄의 판단 그대로다. 규칙도 표면도 바뀌지 않았으므로 I1~I6 은 그대로다
- 2026-09-12 운영 — 설치 도구(`module-harness-init`)와 CI 를 놓는다 (DESIGN.md 7절). 손으로 하던 셋(pre-commit 훅, 루트 CLAUDE.md 계약 문단, 모듈별 어댑터)을 대신하고, 게이트 바이너리에 넣지 않았다: 판정 도구가 파일을 쓰면 I5 가 흐려진다. 어댑터 문구는 스키마에서 읽어 쓴다 — 사본을 두지 않는 것이 새 불변식 I6 이고, 그래서 "바이트 단위로 같게 유지한다" 가 약속이 아니라 구조가 됐다. CI 는 PR 에 `--base`, nightly 에 `--audit` 을 건다(전수 감사는 diff 와 무관해 PR 을 막을 근거가 아니다). 버전은 0.3.0 — 규칙 추가·새 모드는 minor 이고, 이미 통과하던 계약서를 FAIL 시키는 변경은 이번에 없었다. 테스트 일곱을 더했다 (게이트 58, 설치 7, [[loop]] 20)
- 2026-09-12 5단계 — 게이트에 `--review` 를 낸다 (DESIGN.md 6절). 판정하지 않고 종료 코드 0 이라 `--scope` 와 같은 급이다: 리뷰 범위는 diff 가 아니라 **diff 가 건드린 파일을 근거로 인용하는 불변식 전부**이고, 그 목록은 R11 이 이미 걷던 것이라 새 자료구조가 없다. 태그가 리뷰어를 정하므로 스키마의 태그 절에 그 배치를 적었다. `[리뷰]` 태그 상한(DESIGN 6절 미결)은 **규칙으로 만들지 않기로** 했다 — `--review` 가 '전부 [리뷰] 인 계약서' 를 세어 말하지만 막지는 않는다. 계약이 빈약한 것은 고칠 일이지 커밋을 막을 일이 아니고, 규칙으로 만들면 이미 통과하던 소비 리포의 계약서가 갑자기 FAIL 이 된다(7절 버전 표의 첫 줄). I1~I5 는 그대로이고 인용 줄만 밀려서 같이 옮겼다. 테스트 여섯을 더했다 (58개)
- 2026-09-12 4b — 루프를 세우고 `loop/` 의 소유를 [[loop]] 에 넘긴다 (DESIGN.md 5절). 이 계약서에서는 책임 한 줄과 `npm test` 가 두 모듈의 테스트를 함께 돈다는 사실만 바뀐다 — 게이트의 규칙도 표면도 건드리지 않았으므로 I1~I5 는 그대로다. 스키마에는 "계약 갱신은 코드 커밋과 같은 묶음이다" 절을 더했다: observations 의 [규칙 충돌-1] 이 "한 줄이 필요하다" 로 끝난 자리이고, 그 규칙을 기계로 지키는 것이 loop I6 다. 루프를 `files` 화이트리스트에 넣지 않아 패키지 표면은 늘지 않았다 — 첫 소비자가 이 리포 자신이라 아직 내보낼 것이 없다
- 2026-09-12 4a — 게이트에 기계 판독 표면 둘을 낸다 (DESIGN.md 4절). `--json` 은 같은 판정을 JSON 으로 낼 뿐이고 stdout 전용이다: 파일로 쓰지 않으므로 I5 가 서고, 봉투의 `fail` 이 종료 코드와 동치라 부르는 쪽이 JSON 을 읽고 스스로 통과를 선언할 길이 없다. 그 동치를 픽스처가 고정하므로 I2 의 검증 수단을 [grep] 에서 [테스트] 로 올렸다. `--scope <경로>` 는 판정하지 않고 소유 계약과 조상을 깊은 것부터만 낸다 — 조상까지 낼지는 여전히 미결이라(DESIGN 4절) 체인을 통째로 내어 부르는 쪽이 앞에서 자를 수 있게 했다. 소유 판정을 `ownerOf` 에서 `inside` 로 꺼낸 것은 두 표면이 다른 답을 내는 순간 `--scope` 가 거짓말을 하기 때문이다. 테스트 일곱을 더했다 (52개)
- 2026-09-12 분리 잔재를 정리한다 (DESIGN.md 0단계). 사용법 주석·스키마 인용의 `.harness/` 접두사를 `npx module-gate` 와 리포 루트 기준으로 바꿨고, 어느 규칙도 읽지 않던 `MODULE-schema-v0.md` 를 지웠다 — 미결이 물어 온 "언제 버릴지" 의 답이고 히스토리에 남아 있다. `observations/` 의 옛 경로는 그대로 둔다: 관찰은 규칙이 아니라 당시의 증거라 고치면 증거가 아니게 된다
- 2026-09-12 `DESIGN.md` 를 세우고 책임 칸에 그 자리를 적는다 — 분리 이후의 계획(게이트의 기계 판독 출력, scope→change→reconcile 루프, 태그 기반 리뷰, 패키지 운영)을 결정과 근거로 남긴다. `observations/` 가 "무엇이 일어났나" 를 갖고 이 파일이 "무엇을 하기로 했나" 를 가지므로 둘 다 재생성되지 않는 기록이고 같은 급이다. `watch` 에는 넣지 않았다 — 게이트를 고칠 때마다 계획 문서를 함께 요구하게 되고, 그건 계획이 아니라 세금이다
- 2026-09-12 kod-remastered 의 `.harness/` 에서 떨어져 나와 npm 패키지가 됐다 (`module-harness`, bin `module-gate`). 히스토리 31 커밋은 subtree split 으로 따라왔고 `observations/` 도 함께 왔다. 계약서의 자리가 `.harness` 에서 리포 루트로 바뀌어 인용 경로에서 접두사를 벗겼고, `out [[tools]]` 는 없앴다 — 그 모듈은 이제 다른 리포에 있다
- 2026-09-12 R14 를 만든다 — 리포 밖 모듈은 `in` 쪽에서만 `(외부: <패키지명>)` 로 인용하고 설치 목록으로 검증한다. `out` 은 경계를 넘지 못하고(패키지는 소비자를 셀 수 없다), 근거는 `node_modules/` 안을 가리키지 못한다(gitignore 되어 R11 의 diff 에 영원히 안 나온다). 분리를 막고 있던 미결의 답이고, 스키마 필드는 늘지 않았다. 테스트 넷을 더했다 (45개)
- 2026-09-12 R4 가 미결·이력을 빼고 센다. R2 와 R4 가 서로를 밀던 미결의 답이다 — 이력은 줄지 않는 칸이라 함께 세면 R2 가 늘린 줄을 R4 가 벌하고, 오래된 계약서는 고칠 때마다 책임·불변식을 깎게 된다. 12장의 계약 칸은 27~46줄이고 파일 전체로는 38~79줄이었다(engine 이 79). 규칙 넷 중 하나가 바뀌었으므로 스키마 표·3개월 규칙도 같이 고쳤고, 픽스처 테스트 둘을 더했다 (41개)
- 2026-09-12 사람 검토 후 active 로 올린다. 인용 스무 곳을 한 줄씩 열었고 틀린 것은 없었다 — 재현 `from 'node:` 3건, 테스트 39개, 미결의 "트리 범위 주장은 gameplay I2 하나" 도 계약서 전수에서 확인했다(범위를 적은 주장 9개 중 트리 범위는 그 하나뿐이다). 이제 게이트·스키마·테스트를 고치는 커밋은 이 계약서를 함께 요구한다
- 2026-09-12 스키마의 미결을 비우고 질문의 소유를 이 계약서로 옮긴다. 스키마는 규칙서이고 이 파일은 그 규칙서를 가진 디렉토리의 계약이다 — 하네스 자신에 대한 열린 질문(분리 시 리포 밖 슬러그, R2/R4 상충, `watch` 의 `.md` 범위, 자기 판정의 순환)은 여기 있다. 앞 커밋들이 스키마를 고치면서 이 파일의 이력을 늘리지 않아 R1 이 울었고, 그 지적이 맞다
- 2026-09-12 R13 의 grep 예산을 24 → 64 로 올린다. 오늘 editor 계약서를 세우다 24 에 닿아 주장이 조용히 안 세어졌는데, 실측해 보니 비용은 건수가 아니라 범위 폭이었다 — 모듈 범위 25회가 0.19s, Assets 트리 범위 25회가 3.2s 다. 예산으로 막을 것이 아니라 범위를 좁히는 규칙이 비용을 통제한다. 예산 초과 경고 자체는 테스트로 고정했다 (70개 주장 픽스처)
- 2026-09-12 `--audit` 를 만든다 — diff 를 보지 않고 인용 줄이 지금 무엇을 담고 있는지만 전수로 묻는다 (파일 끝을 넘었는지, 범위가 통째로 빈 줄·중괄호뿐인지). R11(b) 가 한 diff 의 hunk 만 보는 한계를 덮는 별도 수단이고, 평소 판정에는 섞이지 않는다. 처음 돌리자 이 리포에서 3건이 나왔다 — 이 계약서 자신의 둘과 engine I2 의 `LeScript.cs:252`(내 작업과 무관하게 전부터 밀려 있던 것)
- 2026-09-12 확장자 없는 근거 인용을 R6·R8·R11·R12 의 눈에 들인다. 경로 조각이 둘 이상이고 리포 루트 기준으로 실재하는 **파일**인 토큰만 줍는다 — 디렉토리와 메뉴 경로(`KoD/Addressables/Setup Infra`)는 떨어지고, 확장자 없는 토큰의 해석 실패는 조용하다(파일 인용이라는 표시가 없으므로 R12 로 고발하지 않는다). tools I8 의 `tools/git-hooks/pre-commit` 과 이 계약서 I2 의 같은 인용이 처음으로 추적된다. 규칙을 켜자 R11(b) 가 이 커밋의 게이트 수정으로 밀린 자기 인용 4건을 잡았고 그 줄번호를 같은 커밋에서 고쳤다
- 2026-09-12 최초 작성 (Claude Code 초안, 검토 전). 스키마 미결의 결정 줄("`책임` 칸에 `observations/` 의 위치를 적는다")을 이 파일이 이행했으므로 그쪽 줄은 삭제됐다
